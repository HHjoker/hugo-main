+++
title = 'Rust 每日学习 Day 73：统计功能与批量操作'
date = '2026-05-16T13:00:00+00:00'
description = '第 13 周第 4 天，继续实战项目：实现任务统计、批量操作、数据导出功能'
tags = ['Rust', '实战项目', '统计报表', '批量操作', '数据导出', 'rustodo']
categories = ['Rust 每日学习']
series = ['Rust 每日学习']
+++

# Rust 每日学习 Day 73：统计功能与批量操作

## 今日概览

| 项目 | 内容 |
|------|------|
| **学习日期** | 2026-05-16（星期六） |
| **阶段** | 第 13 周 · 实战项目（第 4 天） |
| **学习时长** | 约 3 小时 |
| **主题** | 任务统计、批量操作、数据导出 |
| **前置知识** | Day 72（任务状态管理与数据持久化） |

---

## 今日内容

昨天我们完善了任务的状态管理和数据持久化。今天我们来为 `rustodo` 添加统计功能、批量操作和数据的导入导出能力，让这个小工具更加实用。

### 1. 统计模块设计

首先，我们创建一个专门的统计模块，用于分析任务数据：

```rust
// src/stats.rs

use crate::models::{Task, Priority};
use crate::storage::Storage;
use std::collections::HashMap;

#[derive(Debug, Clone, Default)]
pub struct TaskStats {
    pub total: u64,
    pub completed: u64,
    pub pending: u64,
    pub overdue: u64,
    pub by_priority: HashMap<Priority, u64>,
}

impl Storage {
    /// 获取任务统计信息
    pub fn get_stats(&self) -> TaskStats {
        let tasks = self.list_tasks();
        let now = chrono::Utc::now();
        
        let mut stats = TaskStats::default();
        stats.total = tasks.len() as u64;
        
        for task in &tasks {
            if task.completed {
                stats.completed += 1;
            } else {
                stats.pending += 1;
                
                // 检查是否过期
                if let Some(due) = task.due_date {
                    if due < now {
                        stats.overdue += 1;
                    }
                }
            }
            
            // 按优先级统计
            *stats.by_priority.entry(task.priority.clone()).or_insert(0) += 1;
        }
        
        stats
    }
    
    /// 获取完成率
    pub fn get_completion_rate(&self) -> f64 {
        let stats = self.get_stats();
        if stats.total == 0 {
            return 0.0;
        }
        stats.completed as f64 / stats.total as f64 * 100.0
    }
    
    /// 获取本周完成任务数
    pub fn get_weekly_completed(&self) -> u64 {
        let tasks = self.list_tasks();
        let now = chrono::Utc::now();
        let week_ago = now - chrono::Duration::days(7);
        
        tasks.iter()
            .filter(|task| {
                task.completed && 
                task.completed_at.map_or(false, |t| t >= week_ago)
            })
            .count() as u64
    }
}
```

### 2. 批量操作实现

接下来实现批量操作功能：

```rust
// src/commands/batch.rs

use crate::models::{Task, Priority};
use crate::storage::Storage;

#[derive(Debug, Clone)]
pub enum BatchOp {
    Complete(Vec<u64>),           // 批量完成
    Delete(Vec<u64>),              // 批量删除
    SetPriority(Vec<u64>, Priority), // 批量设置优先级
    AddTag(Vec<u64>, String),      // 批量添加标签
    RemoveTag(Vec<u64>, String),   // 批量移除标签
}

impl Storage {
    /// 批量完成任务
    pub fn batch_complete(&mut self, ids: &[u64]) -> Result<Vec<u64>, String> {
        let mut completed_ids = Vec::new();
        
        for &id in ids {
            if let Some(task) = self.find_task_by_id(id) {
                let mut task = task.clone();
                task.mark_completed();
                if self.update_task(&task).is_ok() {
                    completed_ids.push(id);
                }
            }
        }
        
        if completed_ids.is_empty() {
            Err("没有任务被完成".to_string())
        } else {
            Ok(completed_ids)
        }
    }
    
    /// 批量删除任务
    pub fn batch_delete(&mut self, ids: &[u64]) -> Result<u64, String> {
        let mut deleted = 0;
        
        for &id in ids {
            if self.delete_task(id).is_ok() {
                deleted += 1;
            }
        }
        
        if deleted == 0 {
            Err("没有任务被删除".to_string())
        } else {
            Ok(deleted)
        }
    }
    
    /// 批量设置优先级
    pub fn batch_set_priority(&mut self, ids: &[u64], priority: Priority) -> Result<u64, String> {
        let mut updated = 0;
        
        for &id in ids {
            if let Some(mut task) = self.find_task_by_id(id).cloned() {
                task.set_priority(priority.clone());
                if self.update_task(&task).is_ok() {
                    updated += 1;
                }
            }
        }
        
        if updated == 0 {
            Err("没有任务的优先级被更新".to_string())
        } else {
            Ok(updated)
        }
    }
}
```

### 3. 数据导出功能

实现 JSON 和 CSV 格式的导出：

```rust
// src/commands/export.rs

use crate::models::Task;
use crate::storage::Storage;
use std::fs::File;
use std::io::{self, BufWriter, Write};
use std::path::Path;

#[derive(Debug, Clone)]
pub enum ExportFormat {
    Json,
    Csv,
    Tsv,
}

impl Storage {
    /// 导出所有任务到文件
    pub fn export(&self, path: &Path, format: ExportFormat) -> Result<(), String> {
        let tasks = self.list_tasks();
        
        match format {
            ExportFormat::Json => self.export_json(path, &tasks),
            ExportFormat::Csv => self.export_csv(path, &tasks),
            ExportFormat::Tsv => self.export_tsv(path, &tasks),
        }
    }
    
    fn export_json(&self, path: &Path, tasks: &[Task]) -> Result<(), String> {
        let json = serde_json::to_string_pretty(tasks)
            .map_err(|e| format!("JSON 序列化失败: {}", e))?;
        
        let mut file = File::create(path)
            .map_err(|e| format!("无法创建文件: {}", e))?;
        
        file.write_all(json.as_bytes())
            .map_err(|e| format!("写入文件失败: {}", e))?;
        
        Ok(())
    }
    
    fn export_csv(&self, path: &Path, tasks: &[Task]) -> Result<(), String> {
        let file = File::create(path)
            .map_err(|e| format!("无法创建文件: {}", e))?;
        let mut writer = BufWriter::new(file);
        
        // 写入 CSV 头部
        writeln!(writer, "id,content,completed,priority,created_at,due_date,tags")
            .map_err(|e| format!("写入失败: {}", e))?;
        
        // 写入数据行
        for task in tasks {
            let tags = task.tags.join(";");
            let due = task.due_date
                .map(|d| d.format("%Y-%m-%d").to_string())
                .unwrap_or_default();
            
            writeln!(writer, "{},{},{},{:?},{},{},{}",
                task.id,
                Self::escape_csv(&task.content),
                task.completed,
                task.priority,
                task.created_at.format("%Y-%m-%d"),
                due,
                tags
            ).map_err(|e| format!("写入失败: {}", e))?;
        }
        
        Ok(())
    }
    
    fn escape_csv(&self, s: &str) -> String {
        if s.contains(',') || s.contains('"') || s.contains('\n') {
            format!("\"{}\"", s.replace('"', "\"\""))
        } else {
            s.to_string()
        }
    }
    
    fn export_tsv(&self, path: &Path, tasks: &[Task]) -> Result<(), String> {
        let file = File::create(path)
            .map_err(|e| format!("无法创建文件: {}", e))?;
        let mut writer = BufWriter::new(file);
        
        // 写入 TSV 头部
        writeln!(writer, "id\tcontent\tcompleted\tpriority\tcreated_at\tdue_date\ttags")
            .map_err(|e| format!("写入失败: {}", e))?;
        
        // 写入数据行
        for task in tasks {
            let tags = task.tags.join(";");
            let due = task.due_date
                .map(|d| d.format("%Y-%m-%d").to_string())
                .unwrap_or_default();
            
            writeln!(writer, "{}\t{}\t{}\t{:?}\t{}\t{}\t{}",
                task.id,
                task.content,
                task.completed,
                task.priority,
                task.created_at.format("%Y-%m-%d"),
                due,
                tags
            ).map_err(|e| format!("写入失败: {}", e))?;
        }
        
        Ok(())
    }
}
```

### 4. 数据导入功能

实现从 JSON 和 CSV 导入任务：

```rust
// src/commands/import.rs

use crate::models::Task;
use crate::storage::Storage;
use std::fs::File;
use std::io::{self, BufRead, BufReader};
use std::path::Path;

impl Storage {
    /// 从文件导入任务
    pub fn import(&mut self, path: &Path) -> Result<u64, String> {
        let extension = path.extension()
            .and_then(|e| e.to_str())
            .unwrap_or("");
        
        match extension.to_lowercase().as_str() {
            "json" => self.import_json(path),
            "csv" => self.import_csv(path),
            _ => Err(format!("不支持的文件格式: {}", extension)),
        }
    }
    
    fn import_json(&mut self, path: &Path) -> Result<u64, String> {
        let file = File::open(path)
            .map_err(|e| format!("无法打开文件: {}", e))?;
        
        let tasks: Vec<Task> = serde_json::from_reader(file)
            .map_err(|e| format!("JSON 解析失败: {}", e))?;
        
        let count = tasks.len() as u64;
        for task in tasks {
            self.add_task(task.content.clone())
                .ok();
        }
        
        Ok(count)
    }
    
    fn import_csv(&mut self, path: &Path) -> Result<u64, String> {
        let file = File::open(path)
            .map_err(|e| format!("无法打开文件: {}", e))?;
        let reader = BufReader::new(file);
        let mut lines = reader.lines();
        
        // 跳过表头
        if let Some(Ok(header)) = lines.next() {
            println!("导入 CSV，表头: {}", header);
        }
        
        let mut count = 0u64;
        
        for line in lines {
            let line = line.map_err(|e| format!("读取行失败: {}", e))?;
            if let Some(task) = Self::parse_csv_task(&line, &mut self.next_id) {
                self.add_task(task.content.clone()).ok();
                count += 1;
            }
        }
        
        Ok(count)
    }
    
    fn parse_csv_task(line: &str, id_counter: &mut u64) -> Option<Task> {
        let fields: Vec<&str> = line.split(',').collect();
        if fields.len() < 2 {
            return None;
        }
        
        let content = fields[1].trim_matches('"').to_string();
        if content.is_empty() {
            return None;
        }
        
        Some(Task::new(*id_counter, content))
    }
}
```

### 5. 统计命令实现

```rust
// src/commands/stats.rs

use crate::storage::Storage;
use crate::stats::TaskStats;
use std::collections::HashMap;

pub fn show_stats(storage: &Storage) {
    let stats = storage.get_stats();
    let rate = storage.get_completion_rate();
    let weekly = storage.get_weekly_completed();
    
    println!("\n📊 任务统计报表");
    println!("{}", "=".repeat(40));
    println!("总任务数: {}", stats.total);
    println!("已完成:   {} ({:.1}%)", stats.completed, rate);
    println!("待完成:   {}", stats.pending);
    println!("已过期:   {}", stats.overdue);
    println!("本周完成: {}", weekly);
    
    println!("\n📈 按优先级分布:");
    for (priority, count) in &stats.by_priority {
        let bar = "█".repeat(*count as usize);
        println!("  {:?}: {} {}", priority, count, bar);
    }
}
```

---

## 关键概念理解

### 1. 统计数据的聚合

统计功能本质上是数据的聚合分析：

```rust
// 使用迭代器链式操作进行统计
let stats = tasks.iter()
    .filter(|t| !t.completed)
    .filter(|t| t.due_date.map_or(false, |d| d < now))
    .count();
```

### 2. 批量操作的事务性思考

批量操作需要考虑部分成功的情况：

```rust
// 返回成功和失败的信息
pub struct BatchResult {
    pub success: Vec<u64>,
    pub failed: Vec<(u64, String)>,
}
```

### 3. 数据格式转换

导出时需要注意特殊字符的转义：

```rust
// CSV 中包含逗号、引号换行的处理
fn escape_csv(&self, s: &str) -> String {
    if s.contains(',') || s.contains('"') || s.contains('\n') {
        format!("\"{}\"", s.replace('"', "\"\""))
    } else {
        s.to_string()
    }
}
```

---

## 练习题

1. **完成率计算**：修改 `get_completion_rate()` 函数，返回一个包含完成率、待完成数、已过期数的结构体。

2. **批量操作回滚**：实现批量删除的确认机制，当用户确认后再执行真正的删除操作。

3. **增量导出**：实现只导出新增或修改过的任务的增量导出功能。

4. **导入冲突处理**：当导入的任务 ID 已存在时，提供「跳过/覆盖/新建」三种策略供用户选择。

5. **统计图表**：使用 ASCII 艺术绘制一个简单的任务完成率柱状图。

---

## 明日计划

Day 74 将继续完善 `rustodo` 项目：
- 命令行交互式模式（REPL）
- 任务搜索与过滤
- 配置管理

---

## 参考资源

- [Rust Serialization](https://serde.rs/)
- [ chrono 日期时间处理](https://crates.io/crates/chrono)
- [CSV 处理 crate](https://crates.io/crates/csv)

---

## 今日感悟

今天为 `rustodo` 添加了统计和批量操作功能，让这个工具逐渐变得更加实用。在实现过程中，我深刻体会到：

1. **统计数据是产品的基础** —— 好的统计数据能帮助用户了解自己的任务完成情况
2. **批量操作提升效率** —— 面对多个任务时，批量操作能大大提升用户体验
3. **数据可移植性重要** —— 支持导入导出让用户不担心数据被锁定

从 Day 71 到 Day 73，我们已经为 `rustodo` 实现了：
- CLI 参数解析
- 基础 CRUD 操作
- 任务状态管理
- 数据持久化
- 统计与批量操作

明天我们将添加交互式模式和搜索功能，让这个工具更加完善！

---

> 📅 **学习进度**：第 13 周 / 共 16 周  
> 🔥 **连续学习**：73 天  
> 📖 **下一个主题**：交互式模式与搜索功能