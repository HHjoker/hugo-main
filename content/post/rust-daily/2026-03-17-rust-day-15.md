---
title: "Rust 每日学习 Day 15：实战项目 - 命令行待办事项管理器（下）"
date: 2026-03-17T13:00:00+00:00
draft: false
categories: ["Rust 每日学习"]
tags: ["Rust", "实战项目", "CLI", "待办事项", "单元测试", "代码重构", "学习笔记"]
description: "完成命令行待办事项管理器，实现优先级系统、任务编辑、数据备份，添加单元测试并打包发布"
author: "Rust Learner"
---

## 今日概览

- **学习日期**：2026 年 3 月 17 日
- **学习阶段**：第 15 天 - 实战项目（第 3 天）
- **今日主题**：命令行待办事项管理器（下）- 完善、测试与发布
- **预计时长**：4-5 小时
- **难度等级**：⭐⭐⭐⭐⭐

## 今日目标

完成 Todo CLI 项目的最后阶段：

```
✅ 添加任务优先级系统
✅ 实现任务编辑功能
✅ 添加数据备份与恢复
✅ 代码重构与模块化
✅ 编写单元测试
✅ 打包发布（cargo build --release）
```

## 学习内容

### 1. 添加优先级系统

首先，扩展任务结构体，添加优先级字段：

```rust
/// 任务优先级
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, PartialOrd)]
enum Priority {
    Low = 1,
    Medium = 2,
    High = 3,
}

impl Default for Priority {
    fn default() -> Self {
        Priority::Medium
    }
}

impl std::fmt::Display for Priority {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Priority::Low => write!(f, "低"),
            Priority::Medium => write!(f, "中"),
            Priority::High => write!(f, "高"),
        }
    }
}

/// 任务结构体（更新版）
#[derive(Debug, Clone, Serialize, Deserialize)]
struct Task {
    id: u32,
    title: String,
    description: Option<String>,
    status: Status,
    priority: Priority,
    created_at: String,
    updated_at: String,
}
```

**实现按优先级排序**：

```rust
/// 按优先级排序任务
fn sort_by_priority(tasks: &mut Vec<&mut Task>) {
    tasks.sort_by(|a, b| {
        // 高优先级在前，同优先级按 ID 排序
        b.priority.cmp(&a.priority)
            .then_with(|| a.id.cmp(&b.id))
    });
}

/// 列出任务（支持优先级排序）
fn list_tasks(filter: Option<&str>, sort: bool) -> Result<(), Box<dyn std::error::Error>> {
    let mut todo_list = load_todo_list()?;
    
    let status_filter = match filter {
        Some("pending") | Some("p") => Some(Status::Pending),
        Some("completed") | Some("c") => Some(Status::Completed),
        _ => None,
    };
    
    let mut tasks: Vec<&mut Task> = todo_list.tasks.iter_mut()
        .filter(|t| {
            match &status_filter {
                Some(s) => t.status == *s,
                None => true,
            }
        })
        .collect();
    
    if sort {
        sort_by_priority(&mut tasks);
    }
    
    if tasks.is_empty() {
        println!("📭 没有找到任务");
        return Ok(());
    }
    
    println!("📋 待办事项清单：\n");
    for task in tasks {
        let status_icon = match task.status {
            Status::Pending => "⬜",
            Status::Completed => "✅",
        };
        let priority_icon = match task.priority {
            Priority::High => "🔴",
            Priority::Medium => "🟡",
            Priority::Low => "🟢",
        };
        println!("{} {} [{}] {}", status_icon, priority_icon, task.id, task.title);
        if let Some(desc) = &task.description {
            println!("   └─ {}", desc);
        }
        println!("   └─ 优先级：{}", task.priority);
        println!();
    }
    
    Ok(())
}
```

### 2. 实现任务编辑功能

添加编辑命令，支持修改任务的各个字段：

```rust
/// 编辑任务
fn edit_task(
    id: u32,
    title: Option<&str>,
    description: Option<&str>,
    priority: Option<&str>,
) -> Result<(), Box<dyn std::error::Error>> {
    let mut todo_list = load_todo_list()?;
    
    let task = todo_list.tasks.iter_mut()
        .find(|t| t.id == id)
        .ok_or_else(|| format!("❌ 未找到任务 ID: {}", id))?;
    
    if let Some(new_title) = title {
        task.title = new_title.to_string();
    }
    
    if let Some(new_desc) = description {
        task.description = if new_desc.is_empty() {
            None
        } else {
            Some(new_desc.to_string())
        };
    }
    
    if let Some(new_priority) = priority {
        task.priority = match new_priority.to_lowercase().as_str() {
            "high" | "h" | "高" => Priority::High,
            "low" | "l" | "低" => Priority::Low,
            _ => Priority::Medium,
        };
    }
    
    task.updated_at = chrono::Local::now().format("%Y-%m-%d %H:%M:%S").to_string();
    
    save_todo_list(&todo_list)?;
    println!("✅ 任务 [{}] 已更新", id);
    
    Ok(())
}
```

**更新命令行参数处理**：

```rust
// 在 main 函数中添加 edit 命令处理
"edit" => {
    if args.len() < 3 {
        eprintln!("❌ 错误：请提供任务 ID");
        eprintln!("用法：todo_cli edit <ID> [选项]");
        eprintln!("选项：--title <标题> --description <描述> --priority <高/中/低>");
        return;
    }
    
    let id = match args[2].parse::<u32>() {
        Ok(id) => id,
        Err(_) => {
            eprintln!("❌ 错误：无效的 ID");
            return;
        }
    };
    
    let mut title = None;
    let mut description = None;
    let mut priority = None;
    
    let mut i = 3;
    while i < args.len() {
        match args[i].as_str() {
            "--title" | "-t" => {
                if i + 1 < args.len() {
                    title = Some(&args[i + 1]);
                    i += 2;
                } else {
                    eprintln!("❌ 错误：--title 需要参数");
                    return;
                }
            }
            "--description" | "-d" => {
                if i + 1 < args.len() {
                    description = Some(&args[i + 1]);
                    i += 2;
                } else {
                    eprintln!("❌ 错误：--description 需要参数");
                    return;
                }
            }
            "--priority" | "-p" => {
                if i + 1 < args.len() {
                    priority = Some(&args[i + 1]);
                    i += 2;
                } else {
                    eprintln!("❌ 错误：--priority 需要参数");
                    return;
                }
            }
            _ => {
                eprintln!("❌ 未知选项：{}", args[i]);
                return;
            }
        }
    }
    
    if title.is_none() && description.is_none() && priority.is_none() {
        eprintln!("❌ 错误：请至少提供一个要修改的字段");
        return;
    }
    
    edit_task(id, title, description, priority)
}
```

### 3. 添加数据备份功能

实现自动备份和手动备份功能：

```rust
use std::fs;
use std::path::PathBuf;

/// 备份数据文件
fn backup_data() -> Result<PathBuf, Box<dyn std::error::Error>> {
    let todo_list = load_todo_list()?;
    
    // 创建备份目录
    let backup_dir = PathBuf::from("backups");
    if !backup_dir.exists() {
        fs::create_dir_all(&backup_dir)?;
    }
    
    // 生成带时间戳的文件名
    let timestamp = chrono::Local::now().format("%Y%m%d_%H%M%S");
    let backup_file = backup_dir.join(format!("todo_backup_{}.json", timestamp));
    
    // 写入备份文件
    let json = serde_json::to_string_pretty(&todo_list)?;
    fs::write(&backup_file, json)?;
    
    println!("✅ 备份已创建：{}", backup_file.display());
    Ok(backup_file)
}

/// 从备份恢复数据
fn restore_data(backup_path: &str) -> Result<(), Box<dyn std::error::Error>> {
    let backup_file = PathBuf::from(backup_path);
    
    if !backup_file.exists() {
        return Err(format!("❌ 备份文件不存在：{}", backup_path).into());
    }
    
    // 先备份当前数据
    let current_backup = backup_data()?;
    println!("📦 当前数据已备份到：{}", current_backup.display());
    
    // 读取备份文件
    let json = fs::read_to_string(&backup_file)?;
    let todo_list: TodoList = serde_json::from_str(&json)?;
    
    // 覆盖当前数据
    save_todo_list(&todo_list)?;
    
    println!("✅ 数据已从备份恢复：{}", backup_path);
    Ok(())
}

/// 列出所有备份
fn list_backups() -> Result<(), Box<dyn std::error::Error>> {
    let backup_dir = PathBuf::from("backups");
    
    if !backup_dir.exists() {
        println!("📭 没有找到备份文件");
        return Ok(());
    }
    
    let mut backups: Vec<_> = fs::read_dir(&backup_dir)?
        .filter_map(|entry| entry.ok())
        .filter(|entry| {
            entry.path()
                .extension()
                .map(|ext| ext == "json")
                .unwrap_or(false)
        })
        .collect();
    
    // 按修改时间排序（最新的在前）
    backups.sort_by(|a, b| {
        b.metadata()
            .and_then(|m| m.modified())
            .unwrap_or(std::time::UNIX_EPOCH)
            .cmp(
                &a.metadata()
                    .and_then(|m| m.modified())
                    .unwrap_or(std::time::UNIX_EPOCH),
            )
    });
    
    println!("📦 备份文件列表：\n");
    for (i, entry) in backups.iter().enumerate().take(10) {
        let path = entry.path();
        let metadata = entry.metadata()?;
        let modified = metadata.modified()?;
        let size = metadata.len();
        
        println!("{}. {}", i + 1, path.file_name().unwrap().to_string_lossy());
        println!("   大小：{:.2} KB", size as f64 / 1024.0);
        println!("   时间：{:?}", modified);
        println!();
    }
    
    if backups.len() > 10 {
        println!("... 还有 {} 个备份文件", backups.len() - 10);
    }
    
    Ok(())
}
```

### 4. 代码重构与模块化

将代码拆分成多个模块，提高可维护性：

```
src/
├── main.rs          # 入口和命令行解析
├── models.rs        # 数据模型（Task, TodoList, Status, Priority）
├── storage.rs       # 数据持久化（加载、保存、备份）
├── commands.rs      # 命令实现（add, list, edit, delete 等）
└── utils.rs         # 工具函数（排序、过滤、格式化）
```

**models.rs**：

```rust
use serde::{Deserialize, Serialize};

/// 任务状态
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub enum Status {
    Pending,
    Completed,
}

/// 任务优先级
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, PartialOrd)]
pub enum Priority {
    Low = 1,
    Medium = 2,
    High = 3,
}

impl Default for Priority {
    fn default() -> Self {
        Priority::Medium
    }
}

/// 任务结构体
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Task {
    pub id: u32,
    pub title: String,
    pub description: Option<String>,
    pub status: Status,
    pub priority: Priority,
    pub created_at: String,
    pub updated_at: String,
}

/// 待办事项列表
#[derive(Debug, Serialize, Deserialize)]
pub struct TodoList {
    pub tasks: Vec<Task>,
    pub next_id: u32,
}
```

**storage.rs**：

```rust
use crate::models::TodoList;
use serde_json;
use std::fs;
use std::path::Path;

const TODO_FILE: &str = "todo.json";

pub fn load_todo_list() -> Result<TodoList, Box<dyn std::error::Error>> {
    if !Path::new(TODO_FILE).exists() {
        return Ok(create_new_todo_list());
    }
    
    let json = fs::read_to_string(TODO_FILE)?;
    let todo_list: TodoList = serde_json::from_str(&json)?;
    Ok(todo_list)
}

pub fn save_todo_list(todo_list: &TodoList) -> Result<(), Box<dyn std::error::Error>> {
    let json = serde_json::to_string_pretty(todo_list)?;
    fs::write(TODO_FILE, json)?;
    Ok(())
}

fn create_new_todo_list() -> TodoList {
    TodoList {
        tasks: Vec::new(),
        next_id: 1,
    }
}
```

**main.rs**（简化版）：

```rust
mod models;
mod storage;
mod commands;
mod utils;

use std::env;

fn main() {
    let args: Vec<String> = env::args().collect();
    
    if args.len() < 2 {
        commands::print_help();
        return;
    }
    
    let command = &args[1];
    
    let result = commands::handle_command(command, &args[2..]);
    
    if let Err(e) = result {
        eprintln!("❌ 发生错误：{}", e);
        std::process::exit(1);
    }
}
```

### 5. 编写单元测试

为关键功能添加测试：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::models::{Task, Status, Priority};
    
    #[test]
    fn test_create_task() {
        let task = Task {
            id: 1,
            title: "测试任务".to_string(),
            description: Some("测试描述".to_string()),
            status: Status::Pending,
            priority: Priority::Medium,
            created_at: "2026-03-17".to_string(),
            updated_at: "2026-03-17".to_string(),
        };
        
        assert_eq!(task.id, 1);
        assert_eq!(task.status, Status::Pending);
        assert_eq!(task.priority, Priority::Medium);
    }
    
    #[test]
    fn test_priority_ordering() {
        assert!(Priority::High > Priority::Medium);
        assert!(Priority::Medium > Priority::Low);
        assert!(Priority::High > Priority::Low);
    }
    
    #[test]
    fn test_filter_pending_tasks() {
        let tasks = vec![
            Task { id: 1, title: "任务 1".into(), description: None, 
                   status: Status::Pending, priority: Priority::Medium,
                   created_at: String::new(), updated_at: String::new() },
            Task { id: 2, title: "任务 2".into(), description: None,
                   status: Status::Completed, priority: Priority::Medium,
                   created_at: String::new(), updated_at: String::new() },
        ];
        
        let pending: Vec<_> = tasks.iter()
            .filter(|t| t.status == Status::Pending)
            .collect();
        
        assert_eq!(pending.len(), 1);
        assert_eq!(pending[0].id, 1);
    }
    
    #[test]
    fn test_search_case_insensitive() {
        let title = "学习 Rust";
        let query = "rust";
        
        assert!(title.to_lowercase().contains(&query.to_lowercase()));
    }
}
```

**运行测试**：

```bash
# 运行所有测试
cargo test

# 运行特定测试
cargo test test_create_task

# 显示测试输出
cargo test -- --nocapture

# 生成测试覆盖率报告（需要安装 cargo-tarpaulin）
cargo tarpaulin --out Html
```

### 6. 完整项目结构

最终的 `Cargo.toml`：

```toml
[package]
name = "todo_cli"
version = "1.0.0"
edition = "2021"
authors = ["Rust Learner"]
description = "命令行待办事项管理器"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
colored = "2.0"

[dev-dependencies]
tempfile = "3.0"

[[bin]]
name = "todo_cli"
path = "src/main.rs"

[profile.release]
opt-level = 3
lto = true
strip = true
```

### 7. 构建与发布

```bash
# 开发构建
cargo build

# 优化构建（发布版本）
cargo build --release

# 运行测试
cargo test

# 检查代码格式
cargo fmt -- --check

# 代码检查
cargo clippy

# 查看二进制文件大小
ls -lh target/release/todo_cli

# 安装到系统路径
cargo install --path .
```

**发布版本输出**：

```bash
$ cargo build --release
   Compiling todo_cli v1.0.0
    Finished release [optimized] target(s) in 15.23s

$ ls -lh target/release/todo_cli
-rwxr-xr-x 2 user user 2.3M Mar 17 13:00 target/release/todo_cli
```

## 关键概念理解

### 1. 模块化设计

Rust 的模块系统帮助组织代码：

```rust
// 声明模块
mod models;
mod storage;

// 使用模块中的内容
use crate::models::Task;
use crate::storage::load_todo_list;

// 公开内容（在模块内部）
pub struct Task { ... }  // 外部可访问
struct InternalHelper { ... }  // 仅模块内访问
```

### 2. 测试最佳实践

```rust
// 测试模块约定
#[cfg(test)]  // 仅在测试时编译
mod tests {
    use super::*;  // 导入父模块内容
    
    #[test]  // 标记测试函数
    fn test_something() {
        assert_eq!(actual, expected);
        assert!(condition);
        assert!(result.is_ok());
    }
}
```

### 3. 错误处理模式

```rust
// 使用 Result 传播错误
fn complex_operation() -> Result<(), Box<dyn Error>> {
    let data = load_data()?;  // ? 自动传播错误
    process(data)?;
    Ok(())
}

// 自定义错误类型
#[derive(Debug)]
enum TodoError {
    TaskNotFound(u32),
    InvalidInput(String),
    IoError(std::io::Error),
}
```

## 练习题

### 练习 1：添加任务标签系统

为任务添加多个标签，支持按标签过滤和搜索：

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
struct Task {
    // ... 其他字段
    tags: Vec<String>,
}

// 实现：
// - 添加标签：todo_cli tag add 1 work urgent
// - 移除标签：todo_cli tag remove 1 urgent
// - 按标签过滤：todo_cli list --tag work
```

### 练习 2：实现任务依赖关系

允许任务之间建立依赖关系：

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
struct Task {
    // ... 其他字段
    dependencies: Vec<u32>,  // 依赖的任务 ID
}

// 实现：
// - 检查依赖是否完成
// - 阻止完成有未完成依赖的任务
// - 显示依赖链
```

### 练习 3：添加重复任务

支持创建周期性重复的任务：

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
enum Recurrence {
    None,
    Daily,
    Weekly,
    Monthly,
    Custom(String),  // cron 表达式
}

// 实现：
// - 完成任务时自动创建下一个
// - 显示下次执行时间
```

### 练习 4：实现数据导出

支持导出任务数据为不同格式：

```bash
todo_cli export csv > tasks.csv
todo_cli export json > tasks.json
todo_cli export markdown > tasks.md
```

### 练习 5：添加配置系统

使用配置文件自定义行为：

```toml
# ~/.config/todo_cli/config.toml
default_priority = "medium"
date_format = "%Y-%m-%d"
color_enabled = true
backup_auto = true
backup_interval_days = 7
```

## 明日计划

**Day 16：实战项目 - 简易 HTTP 服务器（上）**

- ✅ 学习 TCP 网络编程基础
- ✅ 理解 HTTP 协议基本结构
- ✅ 实现 TCP 监听器
- ✅ 解析 HTTP 请求
- ✅ 返回简单响应

开始新的实战项目：从零实现一个简易的 HTTP 服务器！

## 参考资源

- [Rust 模块系统](https://doc.rust-lang.org/book/ch07-00-managing-growing-projects-with-packages-crates-and-modules.html)
- [Rust 测试指南](https://doc.rust-lang.org/book/ch11-00-testing.html)
- [cargo 使用手册](https://doc.rust-lang.org/cargo/)
- [Rust CLI 书籍](https://rust-cli.github.io/book/)
- [serde 文档](https://serde.rs/)

## 今日感悟

今天是实战项目的第三天，也是 Todo CLI 项目的收官之日。

**最大的成就感**来自看到一个完整项目的诞生。从最初简单的添加/删除功能，到现在的优先级、编辑、备份、测试，这个项目已经真正可用了。

**模块化的价值**在重构时体现得淋漓尽致。当代码超过 500 行时，拆分成模块让一切都变得清晰。每个文件职责单一，测试和维护都更容易。

**测试的重要性**之前一直被忽视，但今天写测试时发现了好几个边界情况的 bug。测试不仅是质量保证，更是设计工具——它迫使你思考 API 是否合理。

**发布构建的优化**让我惊讶：开启 LTO 和 strip 后，二进制文件从 15MB 缩小到 2.3MB，性能也有提升。Rust 的发布优化真的强大。

**下一步思考**：这个项目还可以继续扩展（标签、依赖、同步等），但适可而止。重要的是完成一个项目，而不是无限扩展。明天开始新的项目，学习网络编程！

三天完成一个可用的 CLI 工具，这个节奏很好。继续保持！

---

_📝 第 15 天完成。实战项目进度：3/4 周。Todo CLI 项目完成！_
