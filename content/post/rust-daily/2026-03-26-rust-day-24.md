---
title: "Rust 每日学习 Day 24：错误处理实战模式"
date: 2026-03-26T13:00:00+00:00
draft: false
categories: ["Rust", "编程语言", "系统编程", "错误处理"]
tags: ["Rust", "错误处理", "最佳实践", "设计模式", "学习笔记"]
description: "Rust 错误处理实战：掌握常见错误处理模式，学习错误传播、转换、链式处理，从 0 开始构建健壮的错误处理系统"
author: "Rust Learner"
---

## 今日概览

- **学习日期**：2026 年 3 月 26 日
- **学习阶段**：第 4 周第 3 天 - 错误处理实战
- **今日主题**：错误处理实战模式
- **预计时长**：3-4 小时
- **难度等级**：⭐⭐⭐⭐

## 今日目标

```
✅ 掌握错误传播的多种模式
✅ 学习错误转换与映射技巧
✅ 理解 ? 运算符的底层机制
✅ 实战：构建分层的错误处理系统
✅ 掌握错误处理的最佳实践
```

## 学习内容

### 1. 错误传播模式

#### 1.1 基础传播：? 运算符

```rust
use std::fs::File;
use std::io::{self, Read};

fn read_file_content(path: &str) -> io::Result<String> {
    let mut file = File::open(path)?;  // 如果出错，直接返回
    let mut content = String::new();
    file.read_to_string(&mut content)?;
    Ok(content)
}

// ? 运算符等价于：
fn read_file_content_explicit(path: &str) -> io::Result<String> {
    let mut file = match File::open(path) {
        Ok(f) => f,
        Err(e) => return Err(e),  // 提前返回
    };
    let mut content = String::new();
    match file.read_to_string(&mut content) {
        Ok(_) => Ok(content),
        Err(e) => Err(e),
    }
}
```

**关键点**：
- `?` 只能在返回 `Result` 或 `Option` 的函数中使用
- 成功时解包值，失败时提前返回
- 自动进行错误类型转换（通过 `From` trait）

#### 1.2 错误类型转换

```rust
use std::fs::File;
use std::io::{self, Read};
use std::num::ParseIntError;

// 自定义错误类型
#[derive(Debug)]
enum AppError {
    Io(io::Error),
    Parse(ParseIntError),
    EmptyFile,
}

// 实现 From trait 实现自动转换
impl From<io::Error> for AppError {
    fn from(err: io::Error) -> Self {
        AppError::Io(err)
    }
}

impl From<ParseIntError> for AppError {
    fn from(err: ParseIntError) -> Self {
        AppError::Parse(err)
    }
}

fn read_and_parse(path: &str) -> Result<i32, AppError> {
    let mut content = String::new();
    File::open(path)?.read_to_string(&mut content)?;  // io::Error 自动转为 AppError
    
    if content.trim().is_empty() {
        return Err(AppError::EmptyFile);
    }
    
    let number: i32 = content.trim().parse()?;  // ParseIntError 自动转为 AppError
    Ok(number)
}
```

#### 1.3 使用 map_err 进行手动转换

```rust
use std::fs::File;
use std::io::{self, Read};

#[derive(Debug)]
enum DataError {
    FileNotFound(String),
    ReadFailed(String),
    ParseFailed(String),
}

fn read_config(path: &str) -> Result<String, DataError> {
    let mut file = File::open(path)
        .map_err(|e| DataError::FileNotFound(format!("无法打开 {}: {}", path, e)))?;
    
    let mut content = String::new();
    file.read_to_string(&mut content)
        .map_err(|e| DataError::ReadFailed(format!("读取失败：{}", e)))?;
    
    Ok(content)
}
```

### 2. 错误映射与链式处理

#### 2.1 map 与 map_err

```rust
fn process_numbers(strings: Vec<&str>) -> Vec<i32> {
    strings
        .into_iter()
        .filter_map(|s| s.parse::<i32>().ok())  // 失败的解析被过滤掉
        .collect()
}

fn process_numbers_with_errors(strings: Vec<&str>) -> Result<Vec<i32>, Vec<String>> {
    let (successes, errors): (Vec<_>, Vec<_>) = strings
        .into_iter()
        .map(|s| s.parse::<i32>()
            .map_err(|e| format!("'{}' 解析失败：{}", s, e)))
        .partition(|r| r.is_ok());
    
    if !errors.is_empty() {
        let error_msgs: Vec<String> = errors.into_iter()
            .filter_map(|r| r.err())
            .collect();
        return Err(error_msgs);
    }
    
    Ok(successes.into_iter().filter_map(|r| r.ok()).collect())
}
```

#### 2.2 链式错误处理

```rust
use std::fs;

#[derive(Debug)]
enum ConfigError {
    MissingFile,
    InvalidFormat(String),
    MissingField(String),
}

fn load_config(path: &str) -> Result<Config, ConfigError> {
    fs::read_to_string(path)
        .map_err(|_| ConfigError::MissingFile)?
        .lines()
        .find(|line| line.starts_with("version="))
        .ok_or_else(|| ConfigError::MissingField("version".to_string()))?
        .split('=')
        .nth(1)
        .ok_or_else(|| ConfigError::InvalidFormat("version 格式错误".to_string()))
        .map(|v| Config { version: v.to_string() })
}

#[derive(Debug)]
struct Config {
    version: String,
}
```

### 3. 分层错误处理架构

#### 3.1 领域错误 vs 技术错误

```rust
use std::io;

// 领域错误：业务逻辑相关的错误
#[derive(Debug, PartialEq)]
enum DomainError {
    UserNotFound(String),
    InsufficientPermissions,
    InvalidOperation(String),
}

// 技术错误：底层实现相关的错误
#[derive(Debug)]
enum TechnicalError {
    Database(io::Error),
    Network(String),
    Serialization(String),
}

// 统一的应用错误
#[derive(Debug)]
enum AppError {
    Domain(DomainError),
    Technical(TechnicalError),
}

impl From<DomainError> for AppError {
    fn from(err: DomainError) -> Self {
        AppError::Domain(err)
    }
}

impl From<TechnicalError> for AppError {
    fn from(err: TechnicalError) -> Self {
        AppError::Technical(err)
    }
}

// 使用示例
fn get_user(id: u32) -> Result<User, AppError> {
    // 模拟数据库查询
    if id == 0 {
        return Err(DomainError::UserNotFound("用户 ID 不能为 0".to_string()).into());
    }
    
    // 正常返回
    Ok(User { id, name: "User".to_string() })
}

#[derive(Debug)]
struct User {
    id: u32,
    name: String,
}
```

#### 3.2 错误上下文包装器

```rust
use std::fmt;

// 带上下文的错误包装器
#[derive(Debug)]
struct ContextError<E> {
    context: String,
    source: E,
}

impl<E> ContextError<E> {
    fn new(context: impl Into<String>, source: E) -> Self {
        ContextError {
            context: context.into(),
            source,
        }
    }
}

impl<E: fmt::Display> fmt::Display for ContextError<E> {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(f, "{}: {}", self.context, self.source)
    }
}

impl<E: std::error::Error + 'static> std::error::Error for ContextError<E> {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        Some(&self.source)
    }
}

// 扩展 Result 添加 context 方法
trait Context<T, E> {
    fn context(self, msg: impl Into<String>) -> Result<T, ContextError<E>>;
    fn with_context(self, f: impl FnOnce() -> String) -> Result<T, ContextError<E>>;
}

impl<T, E> Context<T, E> for Result<T, E> {
    fn context(self, msg: impl Into<String>) -> Result<T, ContextError<E>> {
        self.map_err(|e| ContextError::new(msg, e))
    }
    
    fn with_context(self, f: impl FnOnce() -> String) -> Result<T, ContextError<E>> {
        self.map_err(|e| ContextError::new(f(), e))
    }
}

// 使用示例
use std::fs::File;

fn load_data(path: &str) -> Result<String, ContextError<io::Error>> {
    File::open(path)
        .context(format!("无法打开文件 '{}'", path))?
        .bytes()
        .collect::<Result<Vec<u8>, _>>()
        .context("读取文件内容失败")
        .map(|bytes| String::from_utf8_lossy(&bytes).to_string())
}
```

### 4. 实战：完整的错误处理系统

```rust
use std::fs::{self, File};
use std::io::{self, BufRead, BufReader, Write};
use std::path::{Path, PathBuf};

// ============ 错误定义 ============

#[derive(Debug)]
pub enum AppError {
    Config(ConfigError),
    Data(DataError),
    Io(io::Error),
}

#[derive(Debug)]
pub enum ConfigError {
    NotFound(PathBuf),
    InvalidFormat(String),
    MissingField(String),
}

#[derive(Debug)]
pub enum DataError {
    ValidationFailed(String),
    ProcessingFailed(String),
}

// ============ From 实现 ============

impl From<io::Error> for AppError {
    fn from(err: io::Error) -> Self {
        AppError::Io(err)
    }
}

impl From<ConfigError> for AppError {
    fn from(err: ConfigError) -> Self {
        AppError::Config(err)
    }
}

impl From<DataError> for AppError {
    fn from(err: DataError) -> Self {
        AppError::Data(err)
    }
}

// ============ Display 实现 ============

impl std::fmt::Display for AppError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            AppError::Config(e) => write!(f, "配置错误：{}", e),
            AppError::Data(e) => write!(f, "数据错误：{}", e),
            AppError::Io(e) => write!(f, "IO 错误：{}", e),
        }
    }
}

impl std::fmt::Display for ConfigError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            ConfigError::NotFound(path) => write!(f, "配置文件未找到：{}", path.display()),
            ConfigError::InvalidFormat(msg) => write!(f, "配置格式无效：{}", msg),
            ConfigError::MissingField(field) => write!(f, "缺少必需字段：{}", field),
        }
    }
}

impl std::fmt::Display for DataError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            DataError::ValidationFailed(msg) => write!(f, "数据验证失败：{}", msg),
            DataError::ProcessingFailed(msg) => write!(f, "数据处理失败：{}", msg),
        }
    }
}

impl std::error::Error for AppError {
    fn source(&self) -> Option<&(dyn std::error::Error + 'static)> {
        match self {
            AppError::Config(e) => Some(e),
            AppError::Data(e) => Some(e),
            AppError::Io(e) => Some(e),
        }
    }
}

// ============ 业务逻辑 ============

#[derive(Debug)]
pub struct AppConfig {
    pub data_dir: PathBuf,
    pub max_records: usize,
}

impl AppConfig {
    pub fn load(path: &Path) -> Result<Self, AppError> {
        if !path.exists() {
            return Err(ConfigError::NotFound(path.to_path_buf()).into());
        }
        
        let content = fs::read_to_string(path)?;
        let mut data_dir = None;
        let mut max_records = None;
        
        for line in content.lines() {
            let line = line.trim();
            if line.starts_with('#') || line.is_empty() {
                continue;
            }
            
            let parts: Vec<&str> = line.splitn(2, '=').collect();
            if parts.len() != 2 {
                return Err(ConfigError::InvalidFormat(format!("无效行：{}", line)).into());
            }
            
            match parts[0].trim() {
                "data_dir" => data_dir = Some(PathBuf::from(parts[1].trim())),
                "max_records" => {
                    max_records = Some(
                        parts[1].trim().parse().map_err(|_| {
                            ConfigError::InvalidFormat("max_records 必须是数字".to_string())
                        })?
                    );
                }
                _ => {}
            }
        }
        
        Ok(AppConfig {
            data_dir: data_dir.ok_or_else(|| {
                ConfigError::MissingField("data_dir".to_string())
            })?,
            max_records: max_records.ok_or_else(|| {
                ConfigError::MissingField("max_records".to_string())
            })?,
        })
    }
}

pub struct DataProcessor {
    config: AppConfig,
}

impl DataProcessor {
    pub fn new(config: AppConfig) -> Self {
        DataProcessor { config }
    }
    
    pub fn process_file(&self, filename: &str) -> Result<Vec<String>, AppError> {
        let path = self.config.data_dir.join(filename);
        
        let file = File::open(&path)
            .map_err(|e| {
                AppError::Io(io::Error::new(
                    e.kind(),
                    format!("无法打开 {}: {}", path.display(), e)
                ))
            })?;
        
        let reader = BufReader::new(file);
        let mut records = Vec::new();
        
        for (line_num, line_result) in reader.lines().enumerate() {
            if records.len() >= self.config.max_records {
                break;
            }
            
            let line = line_result?;
            let validated = self.validate_line(&line, line_num + 1)?;
            records.push(validated);
        }
        
        Ok(records)
    }
    
    fn validate_line(&self, line: &str, line_num: usize) -> Result<String, AppError> {
        if line.trim().is_empty() {
            return Err(DataError::ValidationFailed(
                format!("第 {} 行为空", line_num)
            ).into());
        }
        
        if line.contains('\0') {
            return Err(DataError::ValidationFailed(
                format!("第 {} 行包含空字符", line_num)
            ).into());
        }
        
        Ok(line.to_string())
    }
    
    pub fn write_report(&self, output_path: &Path, records: &[String]) -> Result<(), AppError> {
        let mut file = File::create(output_path)?;
        
        writeln!(file, "数据处理报告")?;
        writeln!(file, "===============")?;
        writeln!(file, "记录数：{}", records.len())?;
        writeln!(file)?;
        
        for (i, record) in records.iter().enumerate() {
            writeln!(file, "{}. {}", i + 1, record)?;
        }
        
        Ok(())
    }
}

// ============ 主函数 ============

fn main() -> Result<(), AppError> {
    let config_path = Path::new("config.txt");
    
    // 创建示例配置
    let mut config_file = File::create(config_path)?;
    writeln!(config_file, "# 应用配置")?;
    writeln!(config_file, "data_dir = ./data")?;
    writeln!(config_file, "max_records = 100")?;
    
    // 加载配置
    let config = AppConfig::load(config_path)?;
    println!("✓ 配置加载成功");
    println!("  数据目录：{}", config.data_dir.display());
    println!("  最大记录数：{}", config.max_records);
    
    // 创建数据目录和示例数据
    fs::create_dir_all(&config.data_dir)?;
    let data_path = config.data_dir.join("sample.txt");
    let mut data_file = File::create(&data_path)?;
    writeln!(data_file, "记录 1")?;
    writeln!(data_file, "记录 2")?;
    writeln!(data_file, "记录 3")?;
    
    // 处理数据
    let processor = DataProcessor::new(config);
    let records = processor.process_file("sample.txt")?;
    println!("✓ 处理了 {} 条记录", records.len());
    
    // 生成报告
    let report_path = Path::new("report.txt");
    processor.write_report(report_path, &records)?;
    println!("✓ 报告已生成：{}", report_path.display());
    
    Ok(())
}

#[cfg(test)]
mod tests {
    use super::*;
    
    #[test]
    fn test_config_load_missing() {
        let result = AppConfig::load(Path::new("nonexistent.txt"));
        assert!(matches!(result, Err(AppError::Config(ConfigError::NotFound(_)))));
    }
    
    #[test]
    fn test_validation_empty_line() {
        let config = AppConfig {
            data_dir: PathBuf::from("./data"),
            max_records: 100,
        };
        let processor = DataProcessor::new(config);
        let result = processor.validate_line("", 1);
        assert!(matches!(result, Err(AppError::Data(DataError::ValidationFailed(_)))));
    }
}
```

### 5. 错误处理最佳实践

```
┌─────────────────────────────────────────────────────────┐
│  Rust 错误处理最佳实践                                   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  1. 使用 Result 而不是 panic!                           │
│     - panic! 只用于不可恢复的错误                        │
│     - 大部分错误应该用 Result 处理                       │
│                                                         │
│  2. 提供有意义的错误信息                                 │
│     ❌ return Err("failed".into());                    │
│     ✅ return Err(format!("无法连接数据库：{}", e));    │
│                                                         │
│  3. 使用 ? 运算符简化传播                               │
│     - 避免嵌套 match                                     │
│     - 保持代码线性可读                                   │
│                                                         │
│  4. 在边界处转换错误                                     │
│     - 库内部：使用具体错误类型                           │
│     - 库边界：转换为统一的错误类型                       │
│     - 应用层：使用 anyhow 添加上下文                     │
│                                                         │
│  5. 实现 Display 和 Error trait                         │
│     - 让错误可以被打印和链式处理                         │
│     - 提供 source() 方法保留原始错误                     │
│                                                         │
│  6. 使用 thiserror 减少样板代码                         │
│     #[derive(thiserror::Error, Debug)]                 │
│     enum MyError {                                     │
│         #[error("IO 错误：{0}")]                       │
│         Io(#[from] std::io::Error),                    │
│     }                                                  │
│                                                         │
│  7. 分层处理错误                                         │
│     - 底层：技术错误（IO、网络）                         │
│     - 中层：领域错误（业务规则）                         │
│     - 高层：用户友好的错误消息                           │
│                                                         │
│  8. 测试错误路径                                         │
│     - 确保错误被正确处理                                 │
│     - 验证错误信息准确                                   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 关键概念理解

### 1. ? 运算符的工作原理

```
? 运算符展开：

expr?  等价于：

match expr {
    Ok(val) => val,
    Err(err) => return Err(err.into()),  // 注意 .into() 转换
}

对于 Option：

match expr {
    Some(val) => val,
    None => return None,
}
```

### 2. 错误类型转换链

```
底层错误 (io::Error)
       ↓ From::from()
技术错误 (TechnicalError)
       ↓ From::from()
应用错误 (AppError)
       ↓ Display
用户友好消息
```

### 3. 错误处理策略选择

```
┌──────────────────┬──────────────────┬──────────────────┐
│ 场景             │ 推荐方案         │ 理由             │
├──────────────────┼──────────────────┼──────────────────┤
│ 快速原型/脚本    │ anyhow::Result   │ 最小样板代码     │
│ 库 crate         │ thiserror        │ 清晰的错误 API   │
│ 大型应用         │ 自定义错误枚举   │ 精细的错误分类   │
│ 简单工具         │ Box<dyn Error>   │ 灵活且简单       │
│ 需要错误上下文   │ anyhow + context │ 便于调试         │
└──────────────────┴──────────────────┴──────────────────┘
```

## 练习题

### 练习 1：实现自定义错误类型

创建一个文件处理器的错误类型，包含以下错误：
- 文件不存在
- 权限不足
- 格式错误（包含行号）
- 编码错误

要求实现 `Display` 和 `Error` trait。

### 练习 2：错误传播练习

```rust
// 补全以下代码，使用 ? 运算符和适当的错误转换
use std::fs::File;
use std::io::{Read, Write};

#[derive(Debug)]
enum ProcessError {
    Io(std::io::Error),
    Utf8(std::string::FromUtf8Error),
}

fn process_file(input: &str, output: &str) -> Result<(), ProcessError> {
    // TODO: 读取输入文件
    // TODO: 将内容转为大写
    // TODO: 写入输出文件
    // 使用 ? 运算符简化错误处理
}
```

### 练习 3：添加错误上下文

扩展练习 2，为每个错误添加上下文信息：
- 文件路径
- 操作类型（读/写）
- 错误发生时的状态

### 练习 4：分层错误处理

设计一个用户注册系统的错误处理：
- 底层：数据库错误、网络错误
- 中层：验证错误、业务规则错误
- 高层：用户友好的错误消息

实现完整的错误转换链。

### 练习 5：使用 anyhow 重构

将今天实战代码中的自定义错误处理改用 `anyhow` 实现，比较两种方式的优劣。

## 明日计划

**Day 25：错误处理综合实战**

- 综合应用所有错误处理技巧
- 实战项目：构建一个带完整错误处理的 CLI 工具
- 学习错误日志记录和报告
- 第 4 周总结与复习

## 参考资源

- [Rust Book - 错误处理](https://doc.rust-lang.org/book/ch09-00-error-handling.html)
- [anyhow crate 文档](https://docs.rs/anyhow)
- [thiserror crate 文档](https://docs.rs/thiserror)
- [Rust 错误处理最佳实践](https://github.com/shepmaster/rust-error-handling-project)
- [std::error::Error trait](https://doc.rust-lang.org/std/error/trait.Error.html)

## 今日感悟

今天的错误处理实战让我深刻理解了 Rust 错误处理系统的优雅之处。

**最大的收获**：
1. `?` 运算符不仅仅是语法糖，它强制我思考每个操作可能失败的情况
2. 分层错误处理让代码更清晰：底层关心"发生了什么"，高层关心"用户需要知道什么"
3. 实现 `From` trait 虽然前期麻烦，但让错误传播变得无比流畅

**一个顿悟时刻**：
之前我不理解为什么 Rust 要搞这么复杂的错误系统。今天写完整套代码后明白了：
- 强制处理错误 → 减少运行时崩溃
- 类型安全的错误 → 编译器帮我检查是否遗漏
- 错误上下文 → 调试时快速定位问题

**遇到的挑战**：
在实现 `ContextError` 包装器时，泛型和 trait 约束让我困惑了一阵。特别是 `std::error::Error` 的生命周期约束 `'static`，需要理解 Rust 的错误类型通常是静态的。

**明日期待**：
明天要把所有错误处理知识综合起来，做一个完整的 CLI 工具。期待看到理论如何转化为实践！

---

*第 4 周进度：3/7 天 | 错误处理模块正在成型，越来越有 Rust 的感觉了*
