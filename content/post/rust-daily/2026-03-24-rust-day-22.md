---
title: "Rust 每日学习 Day 22：错误处理基础 - Result 与 Option"
date: 2026-03-24T13:00:00+00:00
draft: false
categories: ["Rust", "编程语言", "系统编程", "错误处理"]
tags: ["Rust", "Result", "Option", "错误处理", "枚举", "模式匹配", "学习笔记"]
description: "Rust 错误处理基础：深入理解 Option 和 Result 枚举类型，掌握 Some/None、Ok/Err 模式，学习 ? 运算符、map、and_then 等组合子，从 0 开始构建健壮的错误处理能力"
author: "Rust Learner"
---

## 今日概览

- **学习日期**：2026 年 3 月 24 日
- **学习阶段**：第 4 周第 1 天 - 错误处理入门
- **今日主题**：错误处理基础 - Result 与 Option
- **预计时长**：3-4 小时
- **难度等级**：⭐⭐⭐

## 今日目标

```
✅ 理解 Rust 错误处理哲学
✅ 掌握 Option 枚举类型及使用场景
✅ 掌握 Result 枚举类型及使用场景
✅ 学习 ? 运算符的用法
✅ 掌握常用组合子（map、and_then、unwrap_or 等）
✅ 实战：构建带错误处理的函数
```

## 学习内容

### 1. Rust 错误处理哲学

Rust 将错误分为两类：

```rust
/*
可恢复错误（Recoverable Errors）：
- 文件不存在
- 网络连接超时
- 用户输入无效
- 使用 Option 或 Result 处理

不可恢复错误（Unrecoverable Errors）：
- 数组越界（在 debug 模式）
- 除零错误
- 使用 panic! 处理
*/

// panic! 示例 - 程序立即终止
fn main() {
    panic!("程序崩溃了！");  // ❌ 不要滥用
}
```

**Rust 的设计哲学**：
```
┌─────────────────────────────────────────────────────────┐
│  Rust 错误处理核心思想                                   │
├─────────────────────────────────────────────────────────┤
│  1. 错误是值（Errors are Values）                       │
│     - 错误不是异常，而是可以传递、转换的值              │
│     - 编译器强制你处理错误                              │
│                                                         │
│  2. 显式优于隐式（Explicit over Implicit）              │
│     - 函数签名明确表明可能失败                          │
│     - 调用者必须处理或传播错误                          │
│                                                         │
│  3. 类型安全（Type Safety）                             │
│     - Option 和 Result 是类型，不是魔法                 │
│     - 编译器检查所有分支                                │
└─────────────────────────────────────────────────────────┘
```

### 2. Option 枚举类型

`Option<T>` 表示**可能有值，也可能没有值**：

```rust
// Option 的定义（简化版）
enum Option<T> {
    Some(T),  // 有值
    None,     // 没有值
}

// 使用示例
fn main() {
    let some_number: Option<i32> = Some(5);
    let no_number: Option<i32> = None;
    
    // 模式匹配处理
    match some_number {
        Some(n) => println!("数字是：{}", n),
        None => println!("没有数字"),
    }
}
```

**为什么需要 Option？**

```rust
/*
问题：如何表示"没有值"？

其他语言：
- null / nil（但可能导致空指针异常）
- 特殊值（如 -1，但可能冲突）

Rust 的方案：
- Option<T> 强制你处理"没有值"的情况
- 编译器确保你不会忘记检查
*/

// ❌ 错误：不能直接将 Option<T> 当作 T 使用
fn wrong() {
    let opt: Option<i32> = Some(5);
    // let x: i32 = opt;  // 编译错误！
}

// ✅ 正确：必须显式处理
fn right() {
    let opt: Option<i32> = Some(5);
    
    // 方式 1：模式匹配
    match opt {
        Some(x) => println!("值：{}", x),
        None => println!("空"),
    }
    
    // 方式 2：使用 unwrap（可能 panic）
    // let x = opt.unwrap();  // ⚠️ 如果是 None 会 panic
    
    // 方式 3：提供默认值
    let x = opt.unwrap_or(0);  // 如果是 None，返回 0
}
```

### 3. Option 常用方法

```rust
fn option_methods() {
    let some = Some(5);
    let none: Option<i32> = None;
    
    // is_some / is_none
    assert!(some.is_some());
    assert!(none.is_none());
    
    // unwrap - 获取值，None 时 panic
    assert_eq!(some.unwrap(), 5);
    // none.unwrap();  // ❌ panic!
    
    // unwrap_or - 提供默认值
    assert_eq!(some.unwrap_or(0), 5);
    assert_eq!(none.unwrap_or(0), 0);
    
    // unwrap_or_else - 延迟计算默认值
    let result = none.unwrap_or_else(|| {
        println!("计算默认值...");
        42
    });
    assert_eq!(result, 42);
    
    // expect - 类似 unwrap，但可自定义错误信息
    // some.expect("期望有值");
    // none.expect("期望有值");  // panic: 期望有值
    
    // map - 转换内部的值
    let doubled = some.map(|x| x * 2);
    assert_eq!(doubled, Some(10));
    
    let none_doubled = none.map(|x| x * 2);
    assert_eq!(none_doubled, None);  // None 保持不变
    
    // map_or - map + 默认值
    let result = some.map_or(0, |x| x * 2);
    assert_eq!(result, 10);
    
    let result = none.map_or(0, |x| x * 2);
    assert_eq!(result, 0);
    
    // and_then - 链式调用，返回 Option
    let result = some.and_then(|x| Some(x * 2));
    assert_eq!(result, Some(10));
    
    let result = none.and_then(|x| Some(x * 2));
    assert_eq!(result, None);
    
    // filter - 过滤
    let result = some.filter(|&x| x > 3);
    assert_eq!(result, Some(5));
    
    let result = some.filter(|&x| x > 10);
    assert_eq!(result, None);
    
    // ok_or - 转换为 Result
    let result: Result<i32, &str> = some.ok_or("没有值");
    assert_eq!(result, Ok(5));
    
    let result: Result<i32, &str> = none.ok_or("没有值");
    assert_eq!(result, Err("没有值"));
}
```

### 4. Option 实战示例

```rust
/// 在 HashMap 中查找值
use std::collections::HashMap;

fn find_in_map() {
    let mut scores = HashMap::new();
    scores.insert("Alice", 95);
    scores.insert("Bob", 87);
    
    // get 返回 Option<&V>
    let alice_score: Option<&i32> = scores.get("Alice");
    
    match alice_score {
        Some(score) => println!("Alice 的分数：{}", score),
        None => println!("找不到 Alice"),
    }
    
    // 链式调用
    let bob_score = scores
        .get("Bob")
        .map(|s| s * 2)  // 分数翻倍
        .unwrap_or(0);
    
    println!("Bob 的翻倍分数：{}", bob_score);
}

/// 解析字符串为数字
fn parse_number(input: &str) -> Option<i32> {
    input.parse::<i32>().ok()  // Result -> Option
}

fn parse_example() {
    let valid = parse_number("42");
    assert_eq!(valid, Some(42));
    
    let invalid = parse_number("not a number");
    assert_eq!(invalid, None);
    
    // 提供默认值
    let number = parse_number("invalid").unwrap_or(0);
    assert_eq!(number, 0);
}

/// 查找第一个满足条件的元素
fn find_first() {
    let numbers = vec![1, 2, 3, 4, 5, 6];
    
    // find 返回 Option<&T>
    let first_even = numbers.iter().find(|&&x| x % 2 == 0);
    
    match first_even {
        Some(n) => println!("第一个偶数：{}", n),
        None => println!("没有偶数"),
    }
}
```

### 5. Result 枚举类型

`Result<T, E>` 表示**成功或失败**：

```rust
// Result 的定义（简化版）
enum Result<T, E> {
    Ok(T),   // 成功，包含值
    Err(E),  // 失败，包含错误信息
}

// 使用示例
use std::fs::File;

fn main() {
    // File::open 返回 Result<File, io::Error>
    let file_result = File::open("hello.txt");
    
    match file_result {
        Ok(file) => println!("文件打开成功"),
        Err(error) => println!("打开文件失败：{:?}", error),
    }
}
```

**Result vs Option**：
```
┌─────────────────────────────────────────────────────────┐
│  Option vs Result                                       │
├─────────────────────────────────────────────────────────┤
│  Option<T>                                              │
│  - 表示"有值"或"没有值"                                 │
│  - 没有错误信息                                         │
│  - 用于可选值、查找失败等                               │
│                                                         │
│  Result<T, E>                                           │
│  - 表示"成功"或"失败"                                   │
│  - 包含错误类型 E，可携带错误信息                       │
│  - 用于可能失败的操作（IO、网络、解析等）               │
│                                                         │
│  转换：                                                 │
│  - option.ok_or(err) -> Result                          │
│  - result.ok() -> Option (丢弃错误)                     │
│  - result.err() -> Option<E> (只保留错误)               │
└─────────────────────────────────────────────────────────┘
```

### 6. Result 常用方法

```rust
fn result_methods() {
    let ok: Result<i32, &str> = Ok(5);
    let err: Result<i32, &str> = Err("出错了");
    
    // is_ok / is_err
    assert!(ok.is_ok());
    assert!(err.is_err());
    
    // unwrap - 获取值，Err 时 panic
    assert_eq!(ok.unwrap(), 5);
    // err.unwrap();  // ❌ panic!
    
    // unwrap_or - 提供默认值
    assert_eq!(ok.unwrap_or(0), 5);
    assert_eq!(err.unwrap_or(0), 0);
    
    // unwrap_or_else - 延迟计算
    let result = err.unwrap_or_else(|e| {
        println!("错误：{}", e);
        42
    });
    assert_eq!(result, 42);
    
    // expect - 自定义错误信息
    // ok.expect("期望成功");
    // err.expect("期望成功");  // panic: 期望成功：出错了
    
    // map - 转换 Ok 值
    let doubled = ok.map(|x| x * 2);
    assert_eq!(doubled, Ok(10));
    
    let doubled = err.map(|x| x * 2);
    assert_eq!(doubled, Err("出错了"));  // Err 保持不变
    
    // map_err - 转换错误
    let converted = err.map_err(|e| format!("错误：{}", e));
    assert_eq!(converted, Err("错误：出错了".to_string()));
    
    // and_then - 链式调用
    let result = ok.and_then(|x| Ok(x * 2));
    assert_eq!(result, Ok(10));
    
    let result = err.and_then(|x| Ok(x * 2));
    assert_eq!(result, Err("出错了"));  // 短路
    
    // or_else - Err 时尝试恢复
    let result = err.or_else(|e| Ok(42));
    assert_eq!(result, Ok(42));
    
    let result = ok.or_else(|e| Ok(42));
    assert_eq!(result, Ok(5));  // Ok 保持不变
    
    // unwrap_err - 获取错误，Ok 时 panic
    assert_eq!(err.unwrap_err(), "出错了");
    // ok.unwrap_err();  // ❌ panic!
    
    // unwrap_or_default - 使用默认值
    let result: Result<i32, &str> = Err("error");
    assert_eq!(result.unwrap_or_default(), 0);
}
```

### 7. ? 运算符

`?` 是**错误传播**的语法糖：

```rust
use std::fs::File;
use std::io::{self, Read};

// ❌ 冗长的写法
fn read_file_verbose() -> Result<String, io::Error> {
    let mut file = match File::open("hello.txt") {
        Ok(file) => file,
        Err(e) => return Err(e),
    };
    
    let mut contents = String::new();
    match file.read_to_string(&mut contents) {
        Ok(_) => Ok(contents),
        Err(e) => Err(e),
    }
}

// ✅ 使用 ? 运算符
fn read_file_concise() -> Result<String, io::Error> {
    let mut file = File::open("hello.txt")?;  // 如果 Err，直接返回
    let mut contents = String::new();
    file.read_to_string(&mut contents)?;
    Ok(contents)
}

// ✅ 更简洁的链式调用
fn read_file_chained() -> Result<String, io::Error> {
    let mut contents = String::new();
    File::open("hello.txt")?
        .read_to_string(&mut contents)?;
    Ok(contents)
}
```

**? 运算符的工作原理**：
```rust
/*
? 运算符等价于：

expr?

// 展开为：
match expr {
    Ok(val) => val,
    Err(err) => return Err(err.into()),  // 注意：会调用 Into 转换
}

// 对于 Option：
expr?

// 展开为：
match expr {
    Some(val) => val,
    None => return None,
}
```

**? 的使用限制**：
```rust
// ✅ 只能在返回 Result 或 Option 的函数中使用
fn returns_result() -> Result<i32, String> {
    let value = Some(5)?;  // ✅ 可以
    Ok(value)
}

fn returns_option() -> Option<i32> {
    let value = Ok::<i32, String>(5)?;  // ✅ 可以
    Some(value)
}

// ❌ 不能在 main 中直接使用（除非 main 返回 Result）
fn main() {
    // let file = File::open("hello.txt")?;  // ❌ 编译错误
}

// ✅ main 可以返回 Result
fn main() -> Result<(), Box<dyn std::error::Error>> {
    let file = File::open("hello.txt")?;
    Ok(())
}
```

### 8. 自定义错误类型

```rust
use std::fmt;
use std::error::Error;

// 定义自定义错误类型
#[derive(Debug)]
enum AppError {
    IoError(std::io::Error),
    ParseError(std::num::ParseIntError),
    NotFound(String),
    InvalidInput(String),
}

// 实现 Display trait
impl fmt::Display for AppError {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        match self {
            AppError::IoError(e) => write!(f, "IO 错误：{}", e),
            AppError::ParseError(e) => write!(f, "解析错误：{}", e),
            AppError::NotFound(msg) => write!(f, "未找到：{}", msg),
            AppError::InvalidInput(msg) => write!(f, "无效输入：{}", msg),
        }
    }
}

// 实现 Error trait
impl Error for AppError {
    fn source(&self) -> Option<&(dyn Error + 'static)> {
        match self {
            AppError::IoError(e) => Some(e),
            AppError::ParseError(e) => Some(e),
            _ => None,
        }
    }
}

// 实现 From trait，方便使用 ?
impl From<std::io::Error> for AppError {
    fn from(err: std::io::Error) -> Self {
        AppError::IoError(err)
    }
}

impl From<std::num::ParseIntError> for AppError {
    fn from(err: std::num::ParseIntError) -> Self {
        AppError::ParseError(err)
    }
}

// 使用自定义错误
use std::fs;

fn read_and_parse(path: &str) -> Result<i32, AppError> {
    let contents = fs::read_to_string(path)?;  // 自动转换为 AppError::IoError
    let number: i32 = contents.trim().parse()?;  // 自动转换为 AppError::ParseError
    
    if number < 0 {
        return Err(AppError::InvalidInput("数字不能为负数".to_string()));
    }
    
    Ok(number)
}

fn main() {
    match read_and_parse("number.txt") {
        Ok(n) => println!("读取成功：{}", n),
        Err(e) => println!("错误：{}", e),
    }
}
```

### 9. 实战：构建带错误处理的配置文件读取器

```rust
use std::fs;
use std::collections::HashMap;
use std::error::Error;

#[derive(Debug)]
enum ConfigError {
    FileNotFound(String),
    InvalidFormat(String),
    MissingKey(String),
}

impl std::fmt::Display for ConfigError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            ConfigError::FileNotFound(path) => {
                write!(f, "配置文件不存在：{}", path)
            }
            ConfigError::InvalidFormat(line) => {
                write!(f, "配置格式错误：{}", line)
            }
            ConfigError::MissingKey(key) => {
                write!(f, "缺少必需的配置项：{}", key)
            }
        }
    }
}

impl Error for ConfigError {}

/// 配置结构体
#[derive(Debug)]
struct Config {
    host: String,
    port: u16,
    debug: bool,
}

impl Config {
    /// 从文件加载配置
    fn from_file(path: &str) -> Result<Self, ConfigError> {
        let contents = fs::read_to_string(path)
            .map_err(|_| ConfigError::FileNotFound(path.to_string()))?;
        
        Self::from_string(&contents)
    }
    
    /// 从字符串解析配置
    fn from_string(contents: &str) -> Result<Self, ConfigError> {
        let mut map = HashMap::new();
        
        for line in contents.lines() {
            let line = line.trim();
            
            // 跳过空行和注释
            if line.is_empty() || line.starts_with('#') {
                continue;
            }
            
            // 解析 key=value
            let parts: Vec<&str> = line.splitn(2, '=').collect();
            if parts.len() != 2 {
                return Err(ConfigError::InvalidFormat(line.to_string()));
            }
            
            let key = parts[0].trim();
            let value = parts[1].trim();
            map.insert(key, value);
        }
        
        // 提取并验证配置项
        let host = map
            .get("host")
            .ok_or_else(|| ConfigError::MissingKey("host".to_string()))?
            .to_string();
        
        let port: u16 = map
            .get("port")
            .ok_or_else(|| ConfigError::MissingKey("port".to_string()))?
            .parse()
            .map_err(|_| ConfigError::InvalidFormat("port 必须是数字".to_string()))?;
        
        let debug = map
            .get("debug")
            .map(|s| s == "true")
            .unwrap_or(false);
        
        Ok(Config { host, port, debug })
    }
}

fn main() -> Result<(), Box<dyn Error>> {
    // 创建示例配置文件
    let config_content = r#"
# 服务器配置
host = localhost
port = 8080
debug = true
"#;
    
    // 写入临时文件
    fs::write("config.ini", config_content)?;
    
    // 读取配置
    match Config::from_file("config.ini") {
        Ok(config) => {
            println!("✅ 配置加载成功");
            println!("  Host: {}", config.host);
            println!("  Port: {}", config.port);
            println!("  Debug: {}", config.debug);
        }
        Err(e) => {
            println!("❌ 配置加载失败：{}", e);
        }
    }
    
    // 清理
    fs::remove_file("config.ini")?;
    
    Ok(())
}
```

### 10. 错误处理最佳实践

```rust
/*
最佳实践 1：尽早返回

❌ 嵌套过深
fn process(data: Option<i32>) -> Result<String, Error> {
    if let Some(value) = data {
        if value > 0 {
            // ... 处理
        }
    }
    Ok("done".to_string())
}

✅ 使用 ? 和 early return
fn process(data: Option<i32>) -> Result<String, Error> {
    let value = data.ok_or(Error::NoData)?;
    if value <= 0 {
        return Err(Error::InvalidValue);
    }
    // ... 处理
    Ok("done".to_string())
}
*/

/*
最佳实践 2：提供有意义的错误信息

❌ 模糊的错误
fn read_file() -> Result<String, String> {
    // ...
    Err("出错了".to_string())
}

✅ 具体的错误
#[derive(Debug)]
enum FileError {
    NotFound(String),
    PermissionDenied(String),
    InvalidEncoding(String),
}
*/

/*
最佳实践 3：使用 thiserror 简化错误定义

// Cargo.toml
// [dependencies]
// thiserror = "1.0"

use thiserror::Error;

#[derive(Error, Debug)]
enum AppError {
    #[error("文件未找到：{0}")]
    FileNotFound(String),
    
    #[error("IO 错误")]
    Io(#[from] std::io::Error),
    
    #[error("解析失败：{source}")]
    Parse {
        #[source]
        source: std::num::ParseIntError,
    },
}
*/

/*
最佳实践 4：在边界处处理错误

// 内部函数：传播错误
fn internal_logic() -> Result<Data, AppError> {
    // ... 可能失败的操作
}

// 边界函数：转换为用户友好的错误
fn handle_request() -> HttpResponse {
    match internal_logic() {
        Ok(data) => HttpResponse::Ok(data),
        Err(AppError::FileNotFound(_)) => HttpResponse::NotFound(),
        Err(AppError::Io(_)) => HttpResponse::ServerError(),
        Err(_) => HttpResponse::BadRequest(),
    }
}
*/
```

### 11. 组合子实战：函数式错误处理

```rust
/// 使用组合子处理复杂的数据转换
fn process_user_data(input: Option<String>) -> Result<i32, String> {
    input
        .ok_or("没有输入数据".to_string())?  // Option -> Result
        .trim()
        .parse::<i32>()
        .map_err(|e| format!("解析失败：{}", e))?
        .checked_mul(2)
        .ok_or("计算结果溢出".to_string())
}

/// 链式处理多个可能失败的步骤
fn validate_and_process(
    username: Option<String>,
    age: Option<String>,
    email: Option<String>,
) -> Result<(), Vec<String>> {
    let mut errors = Vec::new();
    
    // 验证用户名
    let username = username
        .filter(|s| !s.is_empty())
        .filter(|s| s.len() >= 3)
        .ok_or_else(|| "用户名至少 3 个字符".to_string());
    
    match username {
        Ok(name) => println!("用户名有效：{}", name),
        Err(e) => errors.push(e),
    }
    
    // 验证年龄
    let age = age
        .and_then(|s| s.parse::<u8>().ok())
        .filter(|&a| a >= 18)
        .ok_or_else(|| "年龄必须 >= 18".to_string());
    
    match age {
        Ok(a) => println!("年龄有效：{}", a),
        Err(e) => errors.push(e),
    }
    
    // 验证邮箱
    let email_valid = email
        .filter(|s| s.contains('@'))
        .is_some();
    
    if !email_valid {
        errors.push("邮箱格式无效".to_string());
    }
    
    if errors.is_empty() {
        Ok(())
    } else {
        Err(errors)
    }
}

fn main() {
    // 测试数据转换
    match process_user_data(Some("  21  ".to_string())) {
        Ok(result) => println!("处理结果：{}", result),
        Err(e) => println!("错误：{}", e),
    }
    
    // 测试验证
    match validate_and_process(
        Some("alice".to_string()),
        Some("25".to_string()),
        Some("alice@example.com".to_string()),
    ) {
        Ok(_) => println!("✅ 所有验证通过"),
        Err(errors) => {
            println!("❌ 验证失败：");
            for error in errors {
                println!("  - {}", error);
            }
        }
    }
}
```

## 关键概念理解

### 1. Option 的本质

```rust
/*
Option<T> 不是"可空的 T"，而是"可能有 T"

关键区别：
- null 是 T 的一个特殊值
- Option<T> 是一个独立的类型，与 T 完全不同

这迫使调用者显式处理"没有值"的情况
*/

// 函数签名说明一切
fn find_user(id: u32) -> Option<User> {
    // 返回类型告诉调用者：可能找不到
}

// 调用者必须处理
match find_user(123) {
    Some(user) => use_user(user),
    None => handle_not_found(),
}
```

### 2. Result 的错误类型选择

```rust
/*
选择错误类型的原则：

1. 简单场景：使用 &str 或 String
   Result<T, String>
   
2. 需要类型区分：使用枚举
   enum AppError { Io, Parse, NotFound }
   
3. 需要错误链：实现 Error trait
   impl Error for AppError { fn source() -> ... }
   
4. 追求简洁：使用 thiserror 宏
*/

// 简单函数
fn parse_number(s: &str) -> Result<i32, String> {
    s.parse().map_err(|e| e.to_string())
}

// 复杂应用
#[derive(Debug)]
enum AppError {
    Database(DbError),
    Network(NetworkError),
    Validation(String),
}
```

### 3. ? 运算符的类型转换

```rust
/*
? 会自动调用 Into::into() 转换错误类型

这意味着：
- 函数返回 Result<T, E>
- ? 用于 Result<U, F>
- 必须满足 F: Into<E>

示例：
*/

fn outer() -> Result<(), Box<dyn std::error::Error>> {
    // io::Error 可以转换为 Box<dyn Error>
    std::fs::File::open("file.txt")?;
    
    // parse::Error 也可以转换
    "42".parse::<i32>()?;
    
    Ok(())
}
```

### 4. 错误传播 vs 错误处理

```rust
/*
何时传播错误（使用 ?）：
- 你无法在当地处理这个错误
- 调用者有更好的上下文来处理
- 错误是"正常"的失败情况

何时处理错误：
- 你可以提供有意义的恢复
- 错误是边界情况，需要特殊处理
- 需要记录日志或监控
*/

// 传播：让调用者决定
fn read_config() -> Result<Config, io::Error> {
    let contents = fs::read_to_string("config.json")?;
    Ok(serde_json::from_str(&contents)?)
}

// 处理：提供默认值
fn get_port() -> u16 {
    read_config()
        .map(|c| c.port)
        .unwrap_or(8080)  // 默认端口
}
```

## 练习题

### 练习 1：Option 基础操作

```rust
// 实现以下函数

// 1. 获取 Option 中的值，如果没有则返回 0
fn get_or_zero(opt: Option<i32>) -> i32 {
    todo!()
}

// 2. 将 Option<i32> 翻倍，如果是 None 保持 None
fn double_option(opt: Option<i32>) -> Option<i32> {
    todo!()
}

// 3. 只有当值大于 10 时才返回 Some，否则返回 None
fn filter_greater_than_10(opt: Option<i32>) -> Option<i32> {
    todo!()
}

// 测试
fn main() {
    assert_eq!(get_or_zero(Some(5)), 5);
    assert_eq!(get_or_zero(None), 0);
    
    assert_eq!(double_option(Some(5)), Some(10));
    assert_eq!(double_option(None), None);
    
    assert_eq!(filter_greater_than_10(Some(15)), Some(15));
    assert_eq!(filter_greater_than_10(Some(5)), None);
}
```

### 练习 2：Result 错误处理

```rust
// 实现一个安全的除法函数
fn safe_divide(a: i32, b: i32) -> Result<i32, String> {
    todo!()
}

// 实现一个解析并计算的函数
fn parse_and_calculate(input: &str) -> Result<i32, String> {
    // 解析输入为 i32，然后乘以 2
    todo!()
}

// 测试
fn main() {
    assert_eq!(safe_divide(10, 2), Ok(5));
    assert!(safe_divide(10, 0).is_err());
    
    assert_eq!(parse_and_calculate("21"), Ok(42));
    assert!(parse_and_calculate("abc").is_err());
}
```

### 练习 3：使用 ? 运算符

```rust
use std::fs;

// 使用 ? 重写以下函数
fn read_number_from_file(path: &str) -> Result<i32, Box<dyn std::error::Error>> {
    let contents = fs::read_to_string(path)?;
    let number: i32 = contents.trim().parse()?;
    Ok(number)
}

// 测试（需要先创建一个测试文件）
fn main() -> Result<(), Box<dyn std::error::Error>> {
    fs::write("test.txt", "42")?;
    let num = read_number_from_file("test.txt")?;
    assert_eq!(num, 42);
    fs::remove_file("test.txt")?;
    Ok(())
}
```

### 练习 4：自定义错误类型

```rust
use std::fmt;

// 定义一个表示验证错误的枚举
#[derive(Debug)]
enum ValidationError {
    TooShort(String, usize),  // 字段名，最小长度
    TooLong(String, usize),   // 字段名，最大长度
    InvalidFormat(String),    // 字段名
}

// TODO: 实现 Display trait

// TODO: 实现 std::error::Error trait

// 使用自定义错误实现验证函数
fn validate_username(username: &str) -> Result<(), ValidationError> {
    todo!()
}

fn main() {
    assert!(validate_username("alice").is_ok());
    assert!(validate_username("ab").is_err());
    assert!(validate_username("a").is_err());
}
```

### 练习 5：组合子链式调用

```rust
// 实现一个复杂的数据处理管道
fn process_data(
    input: Option<String>,
) -> Result<i32, String> {
    // 要求：
    // 1. 如果 input 是 None，返回错误"无输入"
    // 2. 去除前后空格
    // 3. 解析为 i32
    // 4. 如果是负数，返回错误"不能为负数"
    // 5. 乘以 2
    // 6. 如果结果超过 1000，返回 1000
    // 7. 返回最终结果
    
    todo!()
}

fn main() {
    assert_eq!(process_data(Some("  100  ".to_string())), Ok(200));
    assert_eq!(process_data(Some("600".to_string())), Ok(1000));
    assert!(process_data(Some("-5".to_string())).is_err());
    assert!(process_data(None).is_err());
    assert!(process_data(Some("abc".to_string())).is_err());
}
```

## 明日计划

明天将继续错误处理的深入学习：

```
📚 学习内容：
- panic! 与 unwrap 的合理使用
- 错误处理模式（Result 的组合与转换）
- 使用 anyhow 和 thiserror crate
- 实战：构建完整的错误处理系统

🎯 学习目标：
- 理解何时使用 panic!
- 掌握第三方错误处理库
- 设计优雅的错误处理架构
```

## 参考资源

- [Rust Book: Error Handling](https://doc.rust-lang.org/book/ch09-00-error-handling.html)
- [Rust By Example: Option](https://doc.rust-lang.org/rust-by-example