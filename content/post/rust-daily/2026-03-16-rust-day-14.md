---
title: "Rust 每日学习 Day 14：实战项目 - 命令行待办事项管理器（中）"
date: 2026-03-16T13:00:00+00:00
draft: false
categories: ["Rust", "编程语言", "系统编程", "实战项目"]
tags: ["Rust", "实战项目", "CLI", "待办事项", "数据过滤", "命令行工具", "学习笔记"]
description: "继续构建命令行待办事项管理器，实现任务过滤、搜索、统计功能，深入学习枚举匹配与集合操作"
author: "Rust Learner"
---

## 今日概览

- **学习日期**：2026 年 3 月 16 日
- **学习阶段**：第 14 天 - 实战项目（第 2 天）
- **今日主题**：命令行待办事项管理器（中）- 过滤、搜索与统计
- **预计时长**：3-4 小时
- **难度等级**：⭐⭐⭐⭐

## 今日目标

在昨天的基础上，今天我们为 Todo CLI 添加更多实用功能：

```
✅ 按状态过滤任务（只显示未完成/已完成）
✅ 关键词搜索任务
✅ 任务统计信息
✅ 改进错误提示
✅ 添加颜色输出（可选）
```

## 学习内容

### 1. 回顾昨日代码

首先，让我们回顾一下昨天的核心数据结构：

```rust
/// 任务状态枚举
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
enum Status {
    Pending,
    Completed,
}

/// 任务结构体
#[derive(Debug, Clone, Serialize, Deserialize)]
struct Task {
    id: u32,
    title: String,
    description: Option<String>,
    status: Status,
    created_at: String,
}

/// 待办事项列表
#[derive(Debug, Serialize, Deserialize)]
struct TodoList {
    tasks: Vec<Task>,
    next_id: u32,
}
```

### 2. 实现任务过滤功能

添加按状态过滤的功能：

```rust
/// 过滤任务列表
fn filter_tasks(todo_list: &TodoList, status: Option<&Status>) -> Vec<&Task> {
    match status {
        Some(Status::Pending) => {
            todo_list.tasks.iter().filter(|t| t.status == Status::Pending).collect()
        }
        Some(Status::Completed) => {
            todo_list.tasks.iter().filter(|t| t.status == Status::Completed).collect()
        }
        None => {
            todo_list.tasks.iter().collect()
        }
    }
}

/// 列出任务（支持过滤）
fn list_tasks(filter: Option<&str>) -> Result<(), Box<dyn std::error::Error>> {
    let todo_list = load_todo_list()?;
    
    let status_filter = match filter {
        Some("pending") | Some("p") => Some(Status::Pending),
        Some("completed") | Some("c") => Some(Status::Completed),
        _ => None,
    };
    
    let tasks = filter_tasks(&todo_list, status_filter.as_ref());
    
    if tasks.is_empty() {
        println!("📭 没有找到任务");
        return Ok(());
    }
    
    println!("📋 待办事项清单：\n");
    for task in tasks {
        let status_icon = match task.status {
            Status::Pending => "⬜",
            Status::Completed => "✅",
        };
        println!("{} [{}] {}", status_icon, task.id, task.title);
        if let Some(desc) = &task.description {
            println!("   └─ {}", desc);
        }
        println!();
    }
    
    Ok(())
}
```

**关键概念理解**：

1. **`Option<&Status>`**：使用 Option 表示可选的过滤条件
2. **`iter().filter()`**：迭代器适配器，过滤符合条件的元素
3. **`collect()`**：将迭代器结果收集到 Vec 中
4. **模式匹配**：使用 match 处理不同的过滤选项

### 3. 实现关键词搜索

添加搜索功能，支持按标题或描述搜索：

```rust
/// 搜索任务
fn search_tasks(query: &str) -> Result<(), Box<dyn std::error::Error>> {
    let todo_list = load_todo_list()?;
    let query_lower = query.to_lowercase();
    
    let matching_tasks: Vec<&Task> = todo_list.tasks.iter()
        .filter(|task| {
            task.title.to_lowercase().contains(&query_lower) ||
            task.description.as_ref()
                .map(|d| d.to_lowercase().contains(&query_lower))
                .unwrap_or(false)
        })
        .collect();
    
    if matching_tasks.is_empty() {
        println!("🔍 没有找到匹配 \"{}\" 的任务", query);
        return Ok(());
    }
    
    println!("🔍 搜索结果（\"{}\"）：\n", query);
    for task in matching_tasks {
        let status_icon = match task.status {
            Status::Pending => "⬜",
            Status::Completed => "✅",
        };
        println!("{} [{}] {}", status_icon, task.id, task.title);
    }
    
    Ok(())
}
```

**技术要点**：

1. **`to_lowercase()`**：转换为小写实现不区分大小写搜索
2. **`contains()`**：检查字符串是否包含子串
3. **`Option::map()`**：对 Option 中的值进行转换
4. **`unwrap_or()`**：提供默认值

### 4. 实现任务统计

添加统计功能，显示任务概况：

```rust
/// 显示任务统计
fn show_stats() -> Result<(), Box<dyn std::error::Error>> {
    let todo_list = load_todo_list()?;
    
    let total = todo_list.tasks.len();
    let completed = todo_list.tasks.iter()
        .filter(|t| t.status == Status::Completed)
        .count();
    let pending = total - completed;
    
    // 计算完成率
    let completion_rate = if total > 0 {
        (completed as f64 / total as f64) * 100.0
    } else {
        0.0
    };
    
    println!("📊 任务统计");
    println!("═══════════════════════");
    println!("总任务数：    {}", total);
    println!("未完成：      {}", pending);
    println!("已完成：      {}", completed);
    println!("完成率：      {:.1}%", completion_rate);
    println!("═══════════════════════");
    
    // 显示进度条
    if total > 0 {
        let bar_width = 30;
        let filled = ((completed as f64 / total as f64) * bar_width as f64) as usize;
        let empty = bar_width - filled;
        
        print!("进度：        [");
        print!("{}", "█".repeat(filled));
        print!("{}", "░".repeat(empty));
        println!("]");
    }
    
    Ok(())
}
```

### 5. 完善命令行参数处理

更新主函数，支持更多命令：

```rust
use std::env;

fn main() {
    let args: Vec<String> = env::args().collect();
    
    if args.len() < 2 {
        print_help();
        return;
    }
    
    let command = &args[1];
    
    let result = match command.as_str() {
        "add" => {
            if args.len() < 3 {
                eprintln!("❌ 错误：请提供任务标题");
                eprintln!("用法：todo_cli add <标题> [描述]");
                return;
            }
            let title = &args[2];
            let description = args.get(3).map(|s| s.as_str());
            add_task(title, description)
        }
        "list" | "ls" => {
            let filter = args.get(2).map(|s| s.as_str());
            list_tasks(filter)
        }
        "complete" | "done" => {
            if args.len() < 3 {
                eprintln!("❌ 错误：请提供任务 ID");
                eprintln!("用法：todo_cli complete <ID>");
                return;
            }
            match args[2].parse::<u32>() {
                Ok(id) => complete_task(id),
                Err(_) => {
                    eprintln!("❌ 错误：无效的 ID");
                    return;
                }
            }
        }
        "delete" | "rm" => {
            if args.len() < 3 {
                eprintln!("❌ 错误：请提供任务 ID");
                eprintln!("用法：todo_cli delete <ID>");
                return;
            }
            match args[2].parse::<u32>() {
                Ok(id) => delete_task(id),
                Err(_) => {
                    eprintln!("❌ 错误：无效的 ID");
                    return;
                }
            }
        }
        "search" | "find" => {
            if args.len() < 3 {
                eprintln!("❌ 错误：请提供搜索关键词");
                eprintln!("用法：todo_cli search <关键词>");
                return;
            }
            search_tasks(&args[2])
        }
        "stats" => show_stats(),
        "help" | "--help" | "-h" => {
            print_help();
            Ok(())
        }
        _ => {
            eprintln!("❌ 未知命令：{}", command);
            print_help();
            return;
        }
    };
    
    if let Err(e) = result {
        eprintln!("❌ 发生错误：{}", e);
    }
}

/// 打印帮助信息
fn print_help() {
    println!("📋 Todo CLI - 命令行待办事项管理器");
    println!();
    println!("用法：todo_cli <命令> [参数]");
    println!();
    println!("命令：");
    println!("  add <标题> [描述]     添加新任务");
    println!("  list [过滤]          列出任务 (pending/completed)");
    println!("  complete <ID>        标记任务为完成");
    println!("  delete <ID>          删除任务");
    println!("  search <关键词>      搜索任务");
    println!("  stats                显示统计信息");
    println!("  help                 显示此帮助信息");
    println!();
    println!("示例：");
    println!("  todo_cli add \"买牛奶\" \"去超市买全脂牛奶\"");
    println!("  todo_cli list pending");
    println!("  todo_cli complete 1");
    println!("  todo_cli search 牛奶");
    println!("  todo_cli stats");
}
```

### 6. 完整代码整合

将以上功能整合到 `src/main.rs`：

```rust
use serde::{Deserialize, Serialize};
use std::env;
use std::fs;
use std::io::{self, Write};
use std::path::Path;

/// 任务状态枚举
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
enum Status {
    Pending,
    Completed,
}

/// 任务结构体
#[derive(Debug, Clone, Serialize, Deserialize)]
struct Task {
    id: u32,
    title: String,
    description: Option<String>,
    status: Status,
    created_at: String,
}

/// 待办事项列表
#[derive(Debug, Serialize, Deserialize)]
struct TodoList {
    tasks: Vec<Task>,
    next_id: u32,
}

const TODO_FILE: &str = "todo.json";

// ... (之前的函数：create_todo_list, load_todo_list, save_todo_list, add_task)

/// 过滤任务列表
fn filter_tasks(todo_list: &TodoList, status: Option<&Status>) -> Vec<&Task> {
    match status {
        Some(Status::Pending) => {
            todo_list.tasks.iter().filter(|t| t.status == Status::Pending).collect()
        }
        Some(Status::Completed) => {
            todo_list.tasks.iter().filter(|t| t.status == Status::Completed).collect()
        }
        None => todo_list.tasks.iter().collect(),
    }
}

/// 列出任务
fn list_tasks(filter: Option<&str>) -> Result<(), Box<dyn std::error::Error>> {
    let todo_list = load_todo_list()?;
    
    let status_filter = match filter {
        Some("pending") | Some("p") => Some(Status::Pending),
        Some("completed") | Some("c") => Some(Status::Completed),
        _ => None,
    };
    
    let tasks = filter_tasks(&todo_list, status_filter.as_ref());
    
    if tasks.is_empty() {
        println!("📭 没有找到任务");
        return Ok(());
    }
    
    println!("📋 待办事项清单：\n");
    for task in tasks {
        let status_icon = match task.status {
            Status::Pending => "⬜",
            Status::Completed => "✅",
        };
        println!("{} [{}] {}", status_icon, task.id, task.title);
        if let Some(desc) = &task.description {
            println!("   └─ {}", desc);
        }
        println!();
    }
    
    Ok(())
}

/// 搜索任务
fn search_tasks(query: &str) -> Result<(), Box<dyn std::error::Error>> {
    let todo_list = load_todo_list()?;
    let query_lower = query.to_lowercase();
    
    let matching_tasks: Vec<&Task> = todo_list.tasks.iter()
        .filter(|task| {
            task.title.to_lowercase().contains(&query_lower) ||
            task.description.as_ref()
                .map(|d| d.to_lowercase().contains(&query_lower))
                .unwrap_or(false)
        })
        .collect();
    
    if matching_tasks.is_empty() {
        println!("🔍 没有找到匹配 \"{}\" 的任务", query);
        return Ok(());
    }
    
    println!("🔍 搜索结果（\"{}\"）：\n", query);
    for task in matching_tasks {
        let status_icon = match task.status {
            Status::Pending => "⬜",
            Status::Completed => "✅",
        };
        println!("{} [{}] {}", status_icon, task.id, task.title);
    }
    
    Ok(())
}

/// 显示统计
fn show_stats() -> Result<(), Box<dyn std::error::Error>> {
    let todo_list = load_todo_list()?;
    
    let total = todo_list.tasks.len();
    let completed = todo_list.tasks.iter()
        .filter(|t| t.status == Status::Completed)
        .count();
    let pending = total - completed;
    
    let completion_rate = if total > 0 {
        (completed as f64 / total as f64) * 100.0
    } else {
        0.0
    };
    
    println!("📊 任务统计");
    println!("═══════════════════════");
    println!("总任务数：    {}", total);
    println!("未完成：      {}", pending);
    println!("已完成：      {}", completed);
    println!("完成率：      {:.1}%", completion_rate);
    println!("═══════════════════════");
    
    if total > 0 {
        let bar_width = 30;
        let filled = ((completed as f64 / total as f64) * bar_width as f64) as usize;
        let empty = bar_width - filled;
        
        print!("进度：        [");
        print!("{}", "█".repeat(filled));
        print!("{}", "░".repeat(empty));
        println!("]");
    }
    
    Ok(())
}

// ... (complete_task, delete_task 函数保持不变)

fn main() {
    // ... (上面的 main 函数代码)
}
```

## 关键概念理解

### 1. 迭代器适配器

Rust 的迭代器是惰性的，可以链式调用：

```rust
// 链式调用示例
let result = tasks.iter()
    .filter(|t| t.status == Status::Pending)  // 过滤
    .map(|t| &t.title)                         // 转换
    .collect::<Vec<_>>();                      // 收集
```

常用适配器：
- `filter()`：过滤元素
- `map()`：转换元素
- `collect()`：收集到集合
- `count()`：计数
- `find()`：查找第一个匹配项

### 2. Option 的链式操作

```rust
// 传统写法
let desc_lower = match task.description {
    Some(d) => d.to_lowercase(),
    None => String::new(),
};

// Option 链式写法（更优雅）
let desc_lower = task.description
    .as_ref()
    .map(|d| d.to_lowercase())
    .unwrap_or_default();
```

### 3. 借用与所有权

```rust
// &TodoList - 借用，不转移所有权
fn filter_tasks(todo_list: &TodoList, ...) -> Vec<&Task> {
    // 返回任务的引用，避免复制
}

// 使用 iter() 而不是 into_iter()
todo_list.tasks.iter()  // 借用元素
todo_list.tasks.into_iter()  // 移动元素（消耗 Vec）
```

## 练习题

### 练习 1：添加优先级过滤

为任务添加优先级（高/中/低），并实现按优先级过滤：

```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
enum Priority {
    Low,
    Medium,
    High,
}

// 修改 Task 结构体，添加 priority 字段
// 实现 filter_by_priority 函数
```

### 练习 2：实现任务编辑功能

添加 `edit` 命令，允许修改任务的标题或描述：

```bash
todo_cli edit 1 --title "新标题"
todo_cli edit 1 --description "新描述"
todo_cli edit 1 --title "新标题" --description "新描述"
```

### 练习 3：添加截止日期

为任务添加可选的截止日期，并实现：
- 添加任务时设置截止日期
- 显示即将过期的任务（3 天内）
- 显示已过期的任务

### 练习 4：实现任务归档

添加 `archive` 命令，将已完成的任务移动到归档文件：
- `todo_cli archive` - 归档所有已完成任务
- `todo_cli archive 1` - 归档指定 ID 的任务
- `todo_cli list-archived` - 查看归档任务

### 练习 5：添加颜色输出

使用 `colored` crate 为输出添加颜色：

```toml
# Cargo.toml
[dependencies]
colored = "2.0"
```

```rust
use colored::Colorize;

println!("{}", "✅ 任务完成".green());
println!("{}", "❌ 发生错误".red());
println!("{}", "⚠️  警告".yellow());
```

## 明日计划

**Day 15：实战项目 - 命令行待办事项管理器（下）**

- ✅ 添加任务优先级系统
- ✅ 实现任务编辑功能
- ✅ 添加数据备份功能
- ✅ 代码重构与优化
- ✅ 编写单元测试
- ✅ 打包发布（cargo build --release）

## 参考资源

- [Rust 迭代器文档](https://doc.rust-lang.org/std/iter/)
- [Option 类型详解](https://doc.rust-lang.org/std/option/)
- [serde 序列化指南](https://serde.rs/)
- [Rust 命令行应用最佳实践](https://rust-cli.github.io/book/)

## 今日感悟

今天是实战项目的第二天，我们为核心功能添加了更多实用性。

**最大的收获**是理解了 Rust 迭代器的强大之处。链式调用让数据处理变得非常清晰：

```rust
tasks.iter()
    .filter(...)
    .map(...)
    .collect()
```

这种写法不仅简洁，而且类型安全，编译器会帮我们检查每一步的正确性。

**遇到的挑战**是处理 Option 类型的链式调用。一开始总是忘记 `as_ref()`，导致所有权问题。后来理解了：
- `as_ref()` 将 `Option<T>` 转为 `Option<&T>`
- 这样可以在不移动值的情况下操作内部数据

**下一步计划**是继续完善功能，然后开始编写测试。一个好的 CLI 工具不仅要有功能，还要有足够的测试保证质量。

明天我们将完成这个项目的核心功能，并开始考虑发布和部署。期待看到一个完整的、可用的 Rust 命令行工具！

---

_📝 第 14 天完成。实战项目进度：2/4 周_
