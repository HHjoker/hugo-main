---
title: "Rust 每日学习 Day 9：所有权与借用（深入理解）"
date: 2026-03-11T13:00:00+00:00
draft: false
categories: ["Rust", "编程语言", "系统编程"]
tags: ["Rust", "所有权", "借用", "内存安全", "学习笔记"]
description: "深入学习 Rust 的所有权系统和借用机制，理解引用、可变性规则以及常见的所有权模式"
author: "Rust Learner"
---

## 今日概览

- **学习日期**：2026 年 3 月 11 日
- **学习阶段**：第 2 周 - 所有权与借用
- **今日主题**：所有权与借用（深入理解）
- **预计时长**：2-3 小时
- **难度等级**：⭐⭐⭐⭐

## 学习内容

### 1. 所有权规则回顾

Rust 的所有权系统有三个核心规则：

1. 每个值都有一个变量，这个变量是它的所有者
2. 同一时间只能有一个所有者
3. 当所有者离开作用域时，值会被丢弃

```rust
fn main() {
    // s1 是 String 的所有者
    let s1 = String::from("hello");
    
    // 所有权转移给 s2
    let s2 = s1;
    
    // s1 不再有效，这行会编译错误
    // println!("{}", s1);
    
    // s2 是现在的所有者
    println!("{}", s2); // 输出：hello
}
```

### 2. Clone - 深拷贝

如果确实需要保留原值，可以使用 `clone()` 进行深拷贝：

```rust
fn main() {
    let s1 = String::from("hello");
    
    // 深拷贝：s1 和 s2 都有独立的堆内存
    let s2 = s1.clone();
    
    // 两个都可以使用
    println!("s1 = {}, s2 = {}", s1, s2);
    // 输出：s1 = hello, s2 = hello
}
```

### 3. Copy trait

对于存储在栈上的简单类型，Rust 实现了 `Copy` trait，赋值时会自动复制：

```rust
fn main() {
    let x = 5;
    let y = x; // x 实现了 Copy，这里自动复制
    
    // x 仍然可以使用
    println!("x = {}, y = {}", x, y);
    // 输出：x = 5, y = 5
    
    // 基本类型都实现了 Copy
    let a = 3.14;
    let b = a;
    
    let c = true;
    let d = c;
    
    let e = 'A';
    let f = e;
    
    println!("a={}, b={}, c={}, d={}, e={}, f={}", a, b, c, d, e, f);
}
```

### 4. 借用（Borrowing）与引用（References）

借用允许你引用一个值而不获取所有权：

```rust
fn main() {
    let s1 = String::from("hello");
    
    // &s1 创建了一个引用（借用），所有权没有转移
    let len = calculate_length(&s1);
    
    // s1 仍然可以使用
    println!("'{}' 的长度是 {}", s1, len);
    // 输出：'hello' 的长度是 5
}

fn calculate_length(s: &String) -> usize {
    s.len()
}
```

### 5. 可变引用

默认引用是不可变的，可以使用 `&mut` 创建可变引用：

```rust
fn main() {
    let mut s = String::from("hello");
    
    // 可变引用
    change(&mut s);
    
    println!("{}", s); // 输出：hello, world
}

fn change(some_string: &mut String) {
    some_string.push_str(", world");
}
```

### 6. 借用规则

Rust 有严格的借用规则：

1. 在任意给定时刻，你只能拥有以下之一：
   - 一个可变引用
   - 任意数量的不可变引用
2. 引用必须总是有效的

```rust
fn main() {
    let mut s = String::from("hello");
    
    // 多个不可变引用 - 允许
    let r1 = &s;
    let r2 = &s;
    println!("{} and {}", r1, r2);
    
    // 可变引用 - 允许（不可变引用已不再使用）
    let r3 = &mut s;
    println!("{}", r3);
    
    // 下面的代码会编译错误：
    // let r1 = &s;
    // let r2 = &mut s; // 错误！不能同时存在可变和不可变引用
}
```

### 7. 悬垂引用（Dangling References）

Rust 在编译时防止悬垂引用：

```rust
// 下面的代码会编译错误
// fn dangle() -> &String {
//     let s = String::from("hello");
//     &s // 错误：s 在函数结束时被丢弃，返回引用无效
// }

// 正确的做法是返回所有权
fn no_dangle() -> String {
    let s = String::from("hello");
    s // 返回所有权
}

fn main() {
    let s = no_dangle();
    println!("{}", s);
}
```

### 8. 切片（Slices）

切片是对集合中一部分的引用：

```rust
fn main() {
    let s = String::from("hello world");
    
    // 字符串切片
    let hello = &s[0..5];
    let world = &s[6..11];
    
    println!("{} {}", hello, world); // 输出：hello world
    
    // 省略开头或结尾
    let hello2 = &s[..5];
    let world2 = &s[6..];
    let entire = &s[..];
    
    println!("{} {} {}", hello2, world2, entire);
    
    // 切片作为参数
    let first_word = get_first_word(&s);
    println!("第一个单词：{}", first_word);
}

fn get_first_word(s: &str) -> &str {
    let bytes = s.as_bytes();
    
    for (i, &item) in bytes.iter().enumerate() {
        if item == b' ' {
            return &s[0..i];
        }
    }
    
    &s[..]
}
```

## 关键概念理解

### 所有权 vs 借用

| 特性 | 所有权转移 | 借用（引用） |
|------|-----------|-------------|
| 语法 | `let s2 = s1` | `let r = &s1` |
| 原变量 | 不再有效 | 仍然可用 |
| 内存 | 所有权转移 | 不获取所有权 |
| 使用场景 | 需要独占访问 | 只需要读取或临时修改 |

### 可变性规则记忆法

- **"多个读者，一个写者"**：可以同时有多个不可变引用，但只能有一个可变引用
- **"读写互斥"**：有可变引用时，不能有其他任何引用

### 切片的重要性

切片让你可以引用字符串的一部分而不需要复制，这对于性能至关重要。字符串字面量本身就是切片：`&str`。

## 练习题

### 练习 1：所有权转移

```rust
fn main() {
    let s = String::from("hello");
    takes_ownership(s);
    
    // 下面的代码需要修复
    // println!("{}", s);
}

fn takes_ownership(some_string: String) {
    println!("{}", some_string);
}
```

**任务**：修改代码使得 `s` 在调用后仍然可以使用。

### 练习 2：借用练习

```rust
fn main() {
    let mut s = String::from("hello");
    
    let r1 = &s;
    let r2 = &s;
    let r3 = &mut s;
    
    println!("{}, {}, and {}", r1, r2, r3);
}
```

**任务**：修复编译错误，使代码能够正常运行。

### 练习 3：切片练习

编写一个函数，接受字符串切片并返回第二个单词：

```rust
fn second_word(s: &str) -> &str {
    // 实现这个函数
}

fn main() {
    let s = String::from("hello world");
    let word = second_word(&s);
    println!("第二个单词：{}", word); // 应该输出：world
}
```

### 练习 4：理解 Copy

以下哪些类型实现了 Copy trait？为什么 String 没有实现？

```rust
// i32, bool, char, f64, String, Vec<i32>
```

### 练习 5：生命周期思考

为什么下面的代码不能编译？如何修复？

```rust
fn longest(x: &str, y: &str) -> &str {
    if x.len() > y.len() {
        x
    } else {
        y
    }
}
```

## 明日计划

**Day 10：所有权的实际应用模式**

- 函数中的所有权模式
- 返回引用时的生命周期
- `Rc` 和 `RefCell` 简介（多所有权场景）
- 实际代码中的所有权最佳实践

## 参考资源

1. [The Rust Programming Language - Chapter 4](https://doc.rust-lang.org/book/ch04-00-understanding-ownership.html)
2. [Rust By Example - Ownership](https://doc.rust-lang.org/rust-by-example/ownership.html)
3. [Rust Reference - Ownership and Borrowing](https://doc.rust-lang.org/reference/expressions.html)
4. [Visualizing Rust's Ownership System](https://github.com/pretzelhammer/rust-blog/blob/master/posts/visualizing-rusts-memory-model.md)

## 今日感悟

今天深入理解了 Rust 最核心也最具挑战性的概念——所有权系统。刚开始确实觉得束缚很多，但慢慢体会到这种设计的美妙之处：

1. **编译时保证内存安全**：不需要垃圾回收器，也没有手动内存管理的风险
2. **零成本抽象**：借用和切片让高效代码变得简单
3. **清晰的语义**：代码清楚地表达了数据的流动和所有权关系

最大的收获是理解了"多个读者，一个写者"的规则。这个规则看似严格，但实际上反映了现实世界中并发安全的基本原则。

遇到的困难主要是记住何时需要 `clone()`，何时可以使用引用。实践中发现，多写代码、多让编译器报错，是学习所有权最好的方式。

明天继续深入所有权的实际应用，期待看到更多实际代码中的模式！

---

**学习进度**：9/112 天（第 2 周）

**累计学习时长**：约 20 小时

**明日继续**：所有权的实际应用模式 🚀
