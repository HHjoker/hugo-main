+++
title = 'Rust 每日学习 Day 36：测试进阶'
description = '第 7 周第 2 天：Mock 测试、属性测试、基准测试、覆盖率工具'
date = '2026-04-07'
draft = false
tags = ['Rust', '测试', 'Mock', '属性测试', '基准测试', '覆盖率']
categories = ['Rust 每日学习']
series = 'Rust 每日学习'
+++

# Rust 每日学习 Day 36：测试进阶

## 📅 今日概览

| 项目 | 内容 |
|------|------|
| **学习日期** | 2026 年 4 月 7 日 |
| **学习阶段** | 第 7 周 - 测试 |
| **今日主题** | 测试进阶技术 |
| **预计时长** | 3-4 小时 |
| **难度等级** | ⭐⭐⭐⭐ |

---

## 📚 学习内容

### 回顾：昨天的内容

昨天我们学习了测试基础：
- 单元测试的结构和断言宏
- 测试返回 `Result` 的函数
- 测试应该 panic 的代码
- 集成测试和文档测试

今天我们将深入更高级的测试技术！

---

### 测试私有模块和内部实现

#### 在测试模块中访问私有项

Rust 允许在同一模块的测试中访问私有项：

```rust
// src/lib.rs

mod internal {
    // 私有函数
    fn secret_calculation(x: i32) -> i32 {
        x * 42
    }

    // 私有结构体
    struct InternalData {
        value: i32,
    }

    impl InternalData {
        fn new(v: i32) -> Self {
            InternalData { value: v }
        }

        fn get_value(&self) -> i32 {
            self.value
        }
    }

    // 公开 API
    pub fn process(input: i32) -> i32 {
        let data = InternalData::new(input);
        secret_calculation(data.get_value())
    }

    // 测试模块可以访问所有私有项！
    #[cfg(test)]
    mod tests {
        use super::*;

        #[test]
        fn test_secret_calculation() {
            assert_eq!(secret_calculation(1), 42);
            assert_eq!(secret_calculation(2), 84);
        }

        #[test]
        fn test_internal_data() {
            let data = InternalData::new(100);
            assert_eq!(data.get_value(), 100);
        }

        #[test]
        fn test_process_public_api() {
            assert_eq!(process(1), 42);
            assert_eq!(process(2), 84);
        }
    }
}
```

#### 使用 `pub(crate)` 进行模块内测试

```rust
// src/lib.rs

// 只在 crate 内部可见
pub(crate) fn internal_helper(x: i32) -> i32 {
    x * 2
}

// 在集成测试中可以使用
// tests/integration_test.rs
use my_crate::internal_helper;

#[test]
fn test_internal_helper_from_integration() {
    assert_eq!(internal_helper(5), 10);
}
```

#### 使用 `#[path]` 属性组织测试

对于大型模块，可以将测试放在单独文件中：

```rust
// src/calculator.rs

pub fn add(a: i32, b: i32) -> i32 {
    a + b
}

pub fn multiply(a: i32, b: i32) -> i32 {
    a * b
}

// 测试放在单独文件
#[cfg(test)]
#[path = "tests/calculator_tests.rs"]
mod calculator_tests;
```

```rust
// src/tests/calculator_tests.rs

use super::*;

#[test]
fn test_add() {
    assert_eq!(add(2, 3), 5);
}

#[test]
fn test_multiply() {
    assert_eq!(multiply(4, 5), 20);
}
```

---

### Mock 和桩代码（Stubbing）

#### 为什么需要 Mock？

当代码依赖外部资源（数据库、API、文件系统）时，我们需要 Mock 来：
- 隔离被测代码
- 控制测试环境
- 加快测试速度
- 避免副作用

#### 使用 Trait 进行 Mock

```rust
// src/database.rs

// 定义数据库操作的 Trait
pub trait Database {
    fn get_user(&self, id: u32) -> Option<String>;
    fn save_user(&mut self, id: u32, name: &str) -> Result<(), String>;
}

// 真实实现
pub struct RealDatabase {
    connection_string: String,
}

impl Database for RealDatabase {
    fn get_user(&self, id: u32) -> Option<String> {
        // 真实的数据库查询
        unimplemented!()
    }

    fn save_user(&mut self, id: u32, name: &str) -> Result<(), String> {
        // 真实的数据库写入
        unimplemented!()
    }
}

// 业务逻辑
pub struct UserService {
    db: Box<dyn Database>,
}

impl UserService {
    pub fn new(db: Box<dyn Database>) -> Self {
        UserService { db }
    }

    pub fn register_user(&mut self, id: u32, name: &str) -> Result<(), String> {
        // 检查用户是否已存在
        if self.db.get_user(id).is_some() {
            return Err("用户已存在".to_string());
        }
        // 保存新用户
        self.db.save_user(id, name)
    }
}

// Mock 实现
pub struct MockDatabase {
    users: std::collections::HashMap<u32, String>,
}

impl MockDatabase {
    pub fn new() -> Self {
        MockDatabase {
            users: std::collections::HashMap::new(),
        }
    }

    pub fn with_existing_user(id: u32, name: &str) -> Self {
        let mut mock = MockDatabase::new();
        mock.users.insert(id, name.to_string());
        mock
    }
}

impl Database for MockDatabase {
    fn get_user(&self, id: u32) -> Option<String> {
        self.users.get(&id).cloned()
    }

    fn save_user(&mut self, id: u32, name: &str) -> Result<(), String> {
        self.users.insert(id, name.to_string());
        Ok(())
    }
}

// 测试
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_register_new_user() {
        let mock_db = MockDatabase::new();
        let mut service = UserService::new(Box::new(mock_db));

        let result = service.register_user(1, "Alice");
        assert!(result.is_ok());
    }

    #[test]
    fn test_register_existing_user() {
        let mock_db = MockDatabase::with_existing_user(1, "Alice");
        let mut service = UserService::new(Box::new(mock_db));

        let result = service.register_user(1, "Bob");
        assert!(result.is_err());
        assert_eq!(result.unwrap_err(), "用户已存在");
    }
}
```

#### 使用 `mockall` crate（推荐）

`mockall` 是 Rust 中最流行的 Mock 库：

```toml
# Cargo.toml
[dev-dependencies]
mockall = "0.11"
```

```rust
// src/payment.rs

use mockall::automock;

// 使用 automock 自动生成 Mock
#[automock]
pub trait PaymentGateway {
    fn charge(&self, amount: f64, card: &str) -> Result<String, String>;
    fn refund(&self, transaction_id: &str) -> Result<(), String>;
}

// 业务逻辑
pub struct OrderProcessor {
    gateway: Box<dyn PaymentGateway>,
}

impl OrderProcessor {
    pub fn new(gateway: Box<dyn PaymentGateway>) -> Self {
        OrderProcessor { gateway }
    }

    pub fn process_order(&self, amount: f64, card: &str) -> Result<String, String> {
        if amount <= 0.0 {
            return Err("金额必须大于零".to_string());
        }
        self.gateway.charge(amount, card)
    }
}

// 测试
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_process_order_success() {
        let mut mock_gateway = MockPaymentGateway::new();
        
        // 设置期望
        mock_gateway
            .expect_charge()
            .with(mockall::predicate::eq(100.0), mockall::predicate::always())
            .times(1)
            .returning(|_, _| Ok("txn_123".to_string()));

        let processor = OrderProcessor::new(Box::new(mock_gateway));
        let result = processor.process_order(100.0, "4111-1111-1111-1111");
        
        assert!(result.is_ok());
        assert_eq!(result.unwrap(), "txn_123");
    }

    #[test]
    fn test_process_order_invalid_amount() {
        let mock_gateway = MockPaymentGateway::new();
        let processor = OrderProcessor::new(Box::new(mock_gateway));
        
        let result = processor.process_order(-50.0, "4111-1111-1111-1111");
        
        assert!(result.is_err());
        assert_eq!(result.unwrap_err(), "金额必须大于零");
    }

    #[test]
    fn test_process_order_payment_failed() {
        let mut mock_gateway = MockPaymentGateway::new();
        
        mock_gateway
            .expect_charge()
            .returning(|_, _| Err("支付失败".to_string()));

        let processor = OrderProcessor::new(Box::new(mock_gateway));
        let result = processor.process_order(100.0, "4111-1111-1111-1111");
        
        assert!(result.is_err());
        assert_eq!(result.unwrap_err(), "支付失败");
    }
}
```

#### 手动创建桩（Stub）

```rust
// 简单的桩实现
pub struct StubFileSystem {
    files: std::collections::HashMap<String, String>,
}

impl StubFileSystem {
    pub fn new() -> Self {
        StubFileSystem {
            files: std::collections::HashMap::new(),
        }
    }

    pub fn with_file(path: &str, content: &str) -> Self {
        let mut stub = StubFileSystem::new();
        stub.files.insert(path.to_string(), content.to_string());
        stub
    }
}

pub trait FileSystem {
    fn read_file(&self, path: &str) -> Result<String, String>;
    fn write_file(&mut self, path: &str, content: &str) -> Result<(), String>;
}

impl FileSystem for StubFileSystem {
    fn read_file(&self, path: &str) -> Result<String, String> {
        self.files
            .get(path)
            .cloned()
            .ok_or_else(|| format!("文件不存在：{}", path))
    }

    fn write_file(&mut self, path: &str, content: &str) -> Result<(), String> {
        self.files.insert(path.to_string(), content.to_string());
        Ok(())
    }
}
```

---

### 属性测试（Property-Based Testing）

#### 什么是属性测试？

属性测试不是测试具体的输入输出，而是测试代码应该满足的**属性**：

```rust
// 传统测试：测试具体值
#[test]
fn test_reverse() {
    assert_eq!(reverse("hello"), "olleh");
}

// 属性测试：测试属性
// 属性：reverse(reverse(x)) == x
#[test]
fn test_reverse_inverse() {
    for s in generate_strings() {
        let r1 = reverse(&s);
        let r2 = reverse(&r1);
        assert_eq!(r2, s);
    }
}
```

#### 使用 `proptest` crate

```toml
# Cargo.toml
[dev-dependencies]
proptest = "1.4"
```

```rust
// src/properties.rs

use proptest::prelude::*;

// 被测试的函数
pub fn add(a: i32, b: i32) -> i32 {
    a + b
}

pub fn multiply(a: i32, b: i32) -> i32 {
    a * b
}

pub fn reverse_string(s: String) -> String {
    s.chars().rev().collect()
}

pub fn sort_vector(mut v: Vec<i32>) -> Vec<i32> {
    v.sort();
    v
}

// 属性测试
proptest! {
    // 属性 1：加法交换律
    #[test]
    fn test_add_commutative(a in any::<i32>(), b in any::<i32>()) {
        prop_assert_eq!(add(a, b), add(b, a));
    }

    // 属性 2：加法结合律
    #[test]
    fn test_add_associative(
        a in any::<i32>(),
        b in any::<i32>(),
        c in any::<i32>()
    ) {
        prop_assert_eq!(add(add(a, b), c), add(a, add(b, c)));
    }

    // 属性 3：加零不变
    #[test]
    fn test_add_zero_identity(a in any::<i32>()) {
        prop_assert_eq!(add(a, 0), a);
        prop_assert_eq!(add(0, a), a);
    }

    // 属性 4：乘法交换律
    #[test]
    fn test_multiply_commutative(a in any::<i32>(), b in any::<i32>()) {
        prop_assert_eq!(multiply(a, b), multiply(b, a));
    }

    // 属性 5：字符串反转两次等于原字符串
    #[test]
    fn test_reverse_inverse(s in ".*") {
        let r1 = reverse_string(s.clone());
        let r2 = reverse_string(r1.clone());
        prop_assert_eq!(r2, s);
    }

    // 属性 6：排序后的向量长度不变
    #[test]
    fn test_sort_preserves_length(v in prop::collection::vec(any::<i32>(), 0..100)) {
        let sorted = sort_vector(v.clone());
        prop_assert_eq!(sorted.len(), v.len());
    }

    // 属性 7：排序后的向量是有序的
    #[test]
    fn test_sort_is_sorted(v in prop::collection::vec(any::<i32>(), 0..100)) {
        let sorted = sort_vector(v);
        for i in 1..sorted.len() {
            prop_assert!(sorted[i - 1] <= sorted[i]);
        }
    }

    // 属性 8：排序不改变元素总和
    #[test]
    fn test_sort_preserves_sum(v in prop::collection::vec(any::<i32>(), 0..100)) {
        let original_sum: i32 = v.iter().sum();
        let sorted_sum: i32 = sort_vector(v.clone()).iter().sum();
        prop_assert_eq!(original_sum, sorted_sum);
    }
}
```

#### 自定义策略（Strategy）

```rust
use proptest::prelude::*;

// 自定义非空字符串策略
fn non_empty_string() -> impl Strategy<Value = String> {
    ".+".prop_map(|s| s.to_string())
}

// 自定义正整数策略
fn positive_i32() -> impl Strategy<Value = i32> {
    1..i32::MAX
}

// 自定义非零整数策略
fn non_zero_i32() -> impl Strategy<Value = i32> {
    prop_oneof![
        1..i32::MAX,
        i32::MIN..-1,
    ]
}

// 使用自定义策略
proptest! {
    #[test]
    fn test_divide_with_non_zero(s in non_empty_string()) {
        prop_assert!(!s.is_empty());
    }

    #[test]
    fn test_multiply_positive(a in positive_i32(), b in positive_i32()) {
        // 可能溢出，但这是属性测试要发现的！
        let _ = a * b;
    }
}
```

#### 使用 `quickcheck`（替代方案）

```toml
# Cargo.toml
[dev-dependencies]
quickcheck = "1.0"
quickcheck_macros = "1.0"
```

```rust
use quickcheck_macros::quickcheck;

#[quickcheck]
fn test_add_commutative(a: i32, b: i32) -> bool {
    add(a, b) == add(b, a)
}

#[quickcheck]
fn test_reverse_inverse(s: String) -> bool {
    let r1 = reverse_string(s.clone());
    let r2 = reverse_string(r1);
    r2 == s
}
```

---

### 基准测试（Benchmarking）

#### 使用 `criterion` crate

```toml
# Cargo.toml
[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "my_benchmark"
harness = false
```

```rust
// benches/my_benchmark.rs

use criterion::{black_box, criterion_group, criterion_main, Criterion, BenchmarkId};

// 被测试的函数
fn fibonacci_recursive(n: u64) -> u64 {
    match n {
        0 => 0,
        1 => 1,
        _ => fibonacci_recursive(n - 1) + fibonacci_recursive(n - 2),
    }
}

fn fibonacci_iterative(n: u64) -> u64 {
    match n {
        0 => 0,
        1 => 1,
        _ => {
            let mut a = 0;
            let mut b = 1;
            for _ in 2..=n {
                let temp = a + b;
                a = b;
                b = temp;
            }
            b
        }
    }
}

fn fibonacci_memo(n: u64, memo: &mut Vec<u64>) -> u64 {
    if n <= 1 {
        return n;
    }
    if memo[n as usize] != 0 {
        return memo[n as usize];
    }
    memo[n as usize] = fibonacci_memo(n - 1, memo) + fibonacci_memo(n - 2, memo);
    memo[n as usize]
}

// 基准测试组
fn fibonacci_benchmark(c: &mut Criterion) {
    let mut group = c.benchmark_group("Fibonacci");

    for i in &[10, 20, 30] {
        group.bench_with_input(
            BenchmarkId::new("recursive", i),
            i,
            |b, &i| b.iter(|| fibonacci_recursive(black_box(i))),
        );
    }

    for i in &[10, 20, 30, 100, 1000] {
        group.bench_with_input(
            BenchmarkId::new("iterative", i),
            i,
            |b, &i| b.iter(|| fibonacci_iterative(black_box(i))),
        );
    }

    for i in &[10, 20, 30, 100, 1000] {
        group.bench_with_input(
            BenchmarkId::new("memo", i),
            i,
            |b, &i| {
                b.iter(|| {
                    let mut memo = vec![0; (i + 1) as usize];
                    fibonacci_memo(black_box(i), &mut memo)
                })
            },
        );
    }
}

// 字符串操作基准测试
fn string_benchmark(c: &mut Criterion) {
    let mut group = c.benchmark_group("String Operations");

    let test_string = "Hello, World! ".repeat(100);

    group.bench_function("chars_iter", |b| {
        b.iter(|| {
            let s = black_box(&test_string);
            s.chars().count()
        })
    });

    group.bench_function("bytes_iter", |b| {
        b.iter(|| {
            let s = black_box(&test_string);
            s.bytes().count()
        })
    });

    group.bench_function("split_whitespace", |b| {
        b.iter(|| {
            let s = black_box(&test_string);
            s.split_whitespace().collect::<Vec<_>>()
        })
    });
}

// 集合操作基准测试
fn collection_benchmark(c: &mut Criterion) {
    let mut group = c.benchmark_group("Collection Operations");

    let numbers: Vec<i32> = (0..10000).collect();

    group.bench_function("vec_push", |b| {
        b.iter(|| {
            let mut v = Vec::new();
            for i in black_box(&numbers) {
                v.push(i);
            }
            v
        })
    });

    group.bench_function("vec_with_capacity", |b| {
        b.iter(|| {
            let mut v = Vec::with_capacity(numbers.len());
            for i in black_box(&numbers) {
                v.push(i);
            }
            v
        })
    });

    group.bench_function("vec_iter_sum", |b| {
        b.iter(|| {
            black_box(&numbers).iter().sum::<i32>()
        })
    });
}

criterion_group!(
    benches,
    fibonacci_benchmark,
    string_benchmark,
    collection_benchmark
);
criterion_main!(benches);
```

运行基准测试：

```bash
# 运行所有基准测试
$ cargo bench

# 运行特定基准测试
$ cargo bench --bench my_benchmark -- fibonacci/iterative

# 生成报告（在 target/criterion/ 目录）
$ cargo bench
```

#### 基准测试输出示例

```
Fibonacci/recursive/10  time:   [1.2345 µs 1.2456 µs 1.2567 µs]
Fibonacci/recursive/20  time:   [123.45 µs 124.56 µs 125.67 µs]
Fibonacci/iterative/10  time:   [12.345 ns 12.456 ns 12.567 ns]
Fibonacci/iterative/100 time:   [123.45 ns 124.56 ns 125.67 ns]
```

---

### 测试覆盖率工具

#### 使用 `cargo-tarpaulin`

```bash
# 安装
$ cargo install cargo-tarpaulin

# 运行覆盖率测试
$ cargo tarpaulin

# 生成 HTML 报告
$ cargo tarpaulin --output-dir ./tarpaulin-report --html

# 只测试特定包
$ cargo tarpaulin --package my_package

# 排除某些文件
$ cargo tarpaulin --exclude-files src/main.rs
```

#### 使用 `cargo-llvm-cov`（推荐，更现代）

```bash
# 安装
$ cargo install cargo-llvm-cov

# 生成覆盖率报告
$ cargo llvm-cov

# 生成 HTML 报告
$ cargo llvm-cov --html

# 生成 LCov 格式（用于 CI）
$ cargo llvm-cov --lcov --output-path lcov.info

# 查看覆盖率摘要
$ cargo llvm-cov --summary-only
```

#### 覆盖率报告示例

```
Filename                       Regions    Missed Regions     Cover Function  Missed Functions  Executed       Lines      Missed Lines     Cover    Branches   Missed Branches     Cover
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
src/lib.rs                        100            10          90.00%       20                 2          90.00%      500            50    90.00%         50             5    90.00%
src/calculator.rs                  50             5          90.00%       10                 1          90.00%      250            25    90.00%         25             2    92.00%
src/utils.rs                       30             6          80.00%        5                 1          80.00%      150            30    80.00%         15             3    80.00%
-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Total                             180            21          88.33%       35                 4          88.57%      900           105    88.33%         90            10    88.89%
```

---

### 测试组织最佳实践（进阶）

#### 测试辅助函数

```rust
// tests/common.rs 或 src/test_utils.rs

pub fn create_test_user(id: u32, name: &str) -> User {
    User {
        id,
        name: name.to_string(),
        email: format!("{}@test.com", name),
        created_at: chrono::Utc::now(),
    }
}

pub fn setup_test_database() -> TestDatabase {
    TestDatabase::new_in_memory()
}

pub fn mock_http_response(status: u16, body: &str) -> HttpResponse {
    HttpResponse {
        status,
        body: body.to_string(),
        headers: std::collections::HashMap::new(),
    }
}
```

#### 测试夹具（Fixtures）

```rust
// 使用 once_cell 创建共享测试夹具
use once_cell::sync::Lazy;
use std::sync::Mutex;

static TEST_CONFIG: Lazy<Mutex<TestConfig>> = Lazy::new(|| {
    Mutex::new(TestConfig {
        database_url: "sqlite::memory:".to_string(),
        api_key: "test_key".to_string(),
    })
});

fn get_test_config() -> TestConfig {
    TEST_CONFIG.lock().unwrap().clone()
}
```

#### 参数化测试

```rust
// 使用 rstest crate
#[cfg(test)]
mod tests {
    use rstest::rstest;

    #[rstest]
    #[case(2, 3, 5)]
    #[case(0, 0, 0)]
    #[case(-1, 1, 0)]
    #[case(100, 200, 300)]
    fn test_add(#[case] a: i32, #[case] b: i32, #[case] expected: i32) {
        assert_eq!(add(a, b), expected);
    }

    #[rstest]
    #[case("hello", "olleh")]
    #[case("", "")]
    #[case("rust", "tsur")]
    fn test_reverse(#[case] input: &str, #[case] expected: &str) {
        assert_eq!(reverse_string(input), expected);
    }
}
```

---

### 异步代码测试

#### 使用 `tokio-test`

```toml
# Cargo.toml
[dev-dependencies]
tokio = { version = "1", features = ["full"] }
tokio-test = "0.4"
```

```rust
use tokio::test;

#[test]
async fn test_async_function() {
    let result = async_operation().await;
    assert_eq!(result, 42);
}

#[test]
async fn test_async_with_timeout() {
    let result = tokio::time::timeout(
        std::time::Duration::from_secs(5),
        async_operation()
    ).await;
    
    assert!(result.is_ok());
}
```

#### 测试异步流

```rust
use tokio_stream::{self, StreamExt};

#[tokio::test]
async fn test_stream() {
    let mut stream = tokio_stream::iter(vec![1, 2, 3, 4, 5]);
    
    let mut sum = 0;
    while let Some(value) = stream.next().await {
        sum += value;
    }
    
    assert_eq!(sum, 15);
}
```

---

## 🔑 关键概念理解

### 1. Mock vs Stub vs Fake

| 类型 | 用途 | 复杂度 |
|------|------|--------|
| **Mock** | 验证交互（调用了什么方法） | 高 |
| **Stub** | 提供预设响应 | 中 |
| **Fake** | 简化实现（如内存数据库） | 低 |

### 2. 属性测试的优势

```rust
// 传统测试：只能覆盖有限情况
#[test]
fn test_sort() {
    assert_eq!(sort(vec![3, 1, 2]), vec![1, 2, 3]);
}

// 属性测试：自动探索边界情况
proptest! {
    #[test]
    fn test_sort_properties(v in prop::collection::vec(any::<i32>(), 0..100)) {
        let sorted = sort(v.clone());
        // 测试多个属性
        prop_assert_eq!(sorted.len(), v.len()); // 长度不变
        prop_assert!(sorted.windows(2).all(|w| w[0] <= w[1])); // 有序
        prop_assert_eq!(v.iter().sum::<i32>(), sorted.iter().sum::<i32>()); // 总和不变
    }
}
```

### 3. 基准测试的注意事项

```rust
// ❌ 错误：优化可能消除代码
fn bad_benchmark() {
    let result = expensive_function();
    // 编译器可能优化掉
}

// ✅ 正确：使用 black_box
use criterion::black_box;

fn good_benchmark() {
    let result = expensive_function(black_box(input));
    black_box(result); // 防止优化
}
```

### 4. 覆盖率解读

- **行覆盖率（Line Coverage）**：多少行代码被执行
- **分支覆盖率（Branch Coverage）**：多少分支被测试
- **函数覆盖率（Function Coverage）**：多少函数被调用
- **区域覆盖率（Region Coverage）**：更细粒度的代码块覆盖

目标：80%+ 覆盖率通常是合理的，100% 可能过度测试。

---

## 💪 练习题

### 练习 1：实现 Mock 数据库

为以下 Trait 创建 Mock 实现并编写测试：

```rust
pub trait Cache {
    fn get(&self, key: &str) -> Option<String>;
    fn set(&mut self, key: &str, value: &str, ttl_secs: u64);
    fn delete(&mut self, key: &str) -> bool;
    fn clear(&mut self);
}

pub struct CacheService {
    cache: Box<dyn Cache>,
}

impl CacheService {
    pub fn new(cache: Box<dyn Cache>) -> Self {
        CacheService { cache }
    }

    pub fn get_or_fetch(&mut self, key: &str, fetch: impl FnOnce() -> String) -> String {
        match self.cache.get(key) {
            Some(value) => value,
            None => {
                let value = fetch();
                self.cache.set(key, &value, 3600);
                value
            }
        }
    }
}
```

要求：
1. 创建 `MockCache` 结构体
2. 测试 `get_or_fetch` 的缓存命中和未命中情况
3. 使用 `mockall` 或手动实现

### 练习 2：属性测试练习

为以下函数编写属性测试：

```rust
pub fn clamp(value: i32, min: i32, max: i32) -> i32 {
    if value < min {
        min
    } else if value > max {
        max
    } else {
        value
    }
}

pub fn binary_search(arr: &[i32], target: i32) -> Option<usize> {
    let mut left = 0;
    let mut right = arr.len();
    
    while left < right {
        let mid = left + (right - left) / 2;
        if arr[mid] == target {
            return Some(mid);
        } else if arr[mid] < target {
            left = mid + 1;
        } else {
            right = mid;
        }
    }
    None
}
```

要求：
1. `clamp` 的结果总是在 [min, max] 范围内
2. `binary_search` 找到的索引对应的值等于 target
3. 如果 `binary_search` 返回 None，则 target 不在数组中

### 练习 3：基准测试对比

编写基准测试对比以下字符串连接方法：

```rust
fn concat_with_plus(strings: &[&str]) -> String {
    let mut result = String::new();
    for s in strings {
        result = result + s;
    }
    result
}

fn concat_with_format(strings: &[&str]) -> String {
    strings.join("")
}

fn concat_with_push(strings: &[&str]) -> String {
    let mut result = String::new();
    for s in strings {
        result.push_str(s);
    }
    result
}

fn concat_with_writer(strings: &[&str]) -> String {
    use std::fmt::Write;
    let mut result = String::new();
    for s in strings {
        write!(&mut result, "{}", s).unwrap();
    }
    result
}
```

### 练习 4：测试覆盖率实践

1. 创建一个包含多个函数的小项目
2. 使用 `cargo-llvm-cov` 生成覆盖率报告
3. 识别未覆盖的代码
4. 添加测试提高覆盖率到 90%+

### 练习 5：异步代码测试

```rust
pub async fn fetch_user(id: u32) -> Result<User, String> {
    tokio::time::sleep(std::time::Duration::from_millis(100)).await;
    
    if id == 0 {
        Err("无效的用户 ID".to_string())
    } else {
        Ok(User {
            id,
            name: format!("User {}", id),
        })
    }
}

pub async fn fetch_users(ids: Vec<u32>) -> Vec<User> {
    let mut users = Vec::new();
    for id in ids {
        if let Ok(user) = fetch_user(id).await {
            users.push(user);
        }
    }
    users