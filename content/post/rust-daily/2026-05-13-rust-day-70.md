+++
title = 'Rust 每日学习 Day 70：实战项目准备与环境搭建'
date = '2026-05-13T13:00:00+00:00'
description = '第 13 周第 1 天，开启实战项目阶段：命令行工具项目规划、依赖管理、环境配置与项目架构设计'
tags = ['Rust', '实战项目', '命令行工具', '环境搭建', '项目架构', 'cargo']
categories = ['Rust 每日学习']
series = ['Rust 每日学习']
+++

# Rust 每日学习 Day 70：实战项目准备与环境搭建

## 今日概览

| 项目 | 内容 |
|------|------|
| **学习日期** | 2026-05-13（星期三） |
| **阶段** | 第 13 周 · 实战项目（第 1 天） |
| **学习时长** | 约 3 小时 |
| **主题** | 项目规划、依赖管理、环境配置、架构设计 |
| **前置知识** | Day 1~69（全部基础知识） |

---

## 今日内容

经过 12 周的系统学习，我们已经掌握了 Rust 的核心知识：从基础语法到所有权系统，从错误处理到并发编程，从 unsafe 到 FFI。现在，是时候将这些知识串联起来，动手构建一个真实的项目了。

### 1. 项目选择：命令行工具 `rustodo`

我们将构建一个 **任务管理器命令行工具**，取名 `rustodo`。选择它的原因：

- **实用性强**：日常生活和工作中都能用到
- **复杂度适中**：涵盖 Rust 核心知识点
- **可扩展性好**：可以不断添加新功能

### 2. 项目需求分析

#### 核心功能

```
rustodo add "完成任务 A"          # 添加新任务
rustodo list                       # 列出所有任务
rustodo done <id>                 # 标记任务完成
rustodo delete <id>              # 删除任务
rustodo edit <id> "新内容"       # 编辑任务
rustodo priority <id> high       # 设置优先级
rustodo due <id> 2026-05-20      # 设置截止日期
rustodo search "关键字"          # 搜索任务
rustodo filter --done            # 筛选任务
```

#### 数据结构设计

```rust
use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};

/// 任务优先级
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "lowercase")]
pub enum Priority {
    Low,
    Medium,
    High,
}

impl Default for Priority {
    fn default() -> Self {
        Priority::Medium
    }
}

/// 单个任务
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Task {
    pub id: u64,
    pub content: String,
    pub completed: bool,
    pub priority: Priority,
    pub created_at: DateTime<Utc>,
    pub due_date: Option<DateTime<Utc>>,
    pub completed_at: Option<DateTime<Utc>>,
    pub tags: Vec<String>,
}

impl Task {
    pub fn new(id: u64, content: String) -> Self {
        Task {
            id,
            content,
            completed: false,
            priority: Priority::default(),
            created_at: Utc::now(),
            due_date: None,
            completed_at: None,
            tags: Vec::new(),
        }
    }
    
    pub fn mark_done(&mut self) {
        self.completed = true;
        self.completed_at = Some(Utc::now());
    }
    
    pub fn is_overdue(&self) -> bool {
        if let Some(due) = self.due_date {
            !self.completed && due < Utc::now()
        } else {
            false
        }
    }
}

/// 任务列表（整个应用的数据）
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TaskList {
    pub tasks: Vec<Task>,
    pub next_id: u64,
}

impl Default for TaskList {
    fn default() -> Self {
        TaskList {
            tasks: Vec::new(),
            next_id: 1,
        }
    }
}

impl TaskList {
    pub fn add_task(&mut self, content: String) -> u64 {
        let id = self.next_id;
        self.next_id += 1;
        let task = Task::new(id, content);
        self.tasks.push(task);
        id
    }
    
    pub fn get_task(&self, id: u64) -> Option<&Task> {
        self.tasks.iter().find(|t| t.id == id)
    }
    
    pub fn get_task_mut(&mut self, id: u64) -> Option<&mut Task> {
        self.tasks.iter_mut().find(|t| t.id == id)
    }
    
    pub fn remove_task(&mut self, id: u64) -> Option<Task> {
        if let Some(pos) = self.tasks.iter().position(|t| t.id == id) {
            Some(self.tasks.remove(pos))
        } else {
            None
        }
    }
    
    pub fn filter_completed(&self) -> Vec<&Task> {
        self.tasks.iter().filter(|t| t.completed).collect()
    }
    
    pub fn filter_pending(&self) -> Vec<&Task> {
        self.tasks.iter().filter(|t| !t.completed).collect()
    }
    
    pub fn search(&self, keyword: &str) -> Vec<&Task> {
        let keyword_lower = keyword.to_lowercase();
        self.tasks.iter()
            .filter(|t| t.content.to_lowercase().contains(&keyword_lower))
            .collect()
    }
}
```

### 3. 项目初始化

#### 创建项目

```bash
# 创建新的二进制项目
cargo new rustodo
cd rustodo

# 添加依赖
cargo add clap@4 --features derive
cargo add chrono@0.4 --features serde
cargo add serde@1 --features derive
cargo add serde_json@1
cargo add serde_yaml@0.9
cargo add log@0.4
cargo add env_logger@0.10
cargo add thiserror@1
cargo add anyhow@1
```

#### `Cargo.toml` 配置

```toml
[package]
name = "rustodo"
version = "0.1.0"
edition = "2021"
authors = ["Your Name <your.email@example.com>"]
description = "A command-line task manager built with Rust"
license = "MIT"
repository = "https://github.com/yourname/rustodo"
keywords = ["todo", "task", "cli", "productivity"]
categories = ["command-line-utilities", "productivity"]

[dependencies]
clap = { version = "4", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
log = "0.4"
env_logger = "0.10"
thiserror = "1"
anyhow = "1"

[dev-dependencies]
assert_cmd = "1"
predicates = "3"
 tempfile = "3"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
```

#### `build.rs` 配置（可选）

```rust
// build.rs
fn main() {
    // 如果需要编译时检查或生成代码，放在这里
    println!("cargo:rerun-if-changed=src/");
}
```

### 4. 目录结构设计

```
rustodo/
├── Cargo.toml
├── build.rs                    # 构建脚本（可选）
├── src/
│   ├── main.rs               # 程序入口
│   ├── lib.rs                # 库入口（单元测试）
│   ├── cli.rs                # CLI 定义（clap）
│   ├── commands/
│   │   ├── mod.rs
│   │   ├── add.rs            # add 子命令
│   │   ├── list.rs           # list 子命令
│   │   ├── done.rs           # done 子命令
│   │   ├── delete.rs         # delete 子命令
│   │   ├── edit.rs          # edit 子命令
│   │   └── search.rs        # search 子命令
│   ├── models/
│   │   ├── mod.rs
│   │   ├── task.rs          # Task 模型
│   │   └── task_list.rs     # TaskList 模型
│   ├── storage/
│   │   ├── mod.rs
│   │   └── file.rs          # 文件持久化
│   ├── errors.rs            # 错误类型定义
│   └── context.rs           # 全局上下文
├── tests/
│   └── integration_tests.rs # 集成测试
├── examples/                 # 示例代码
└── README.md
```

### 5. 模块划分与代码组织

#### `src/models/task.rs` - 任务模型

```rust
use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "lowercase")]
pub enum Priority {
    Low,
    Medium,
    High,
}

impl Default for Priority {
    fn default() -> Self {
        Priority::Medium
    }
}

impl std::fmt::Display for Priority {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Priority::Low => write!(f, "low"),
            Priority::Medium => write!(f, "medium"),
            Priority::High => write!(f, "high"),
        }
    }
}

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct Task {
    pub id: u64,
    pub content: String,
    pub completed: bool,
    pub priority: Priority,
    pub created_at: DateTime<Utc>,
    pub due_date: Option<DateTime<Utc>>,
    pub completed_at: Option<DateTime<Utc>>,
    pub tags: Vec<String>,
}

impl Task {
    pub fn new(id: u64, content: String) -> Self {
        Task {
            id,
            content,
            completed: false,
            priority: Priority::default(),
            created_at: Utc::now(),
            due_date: None,
            completed_at: None,
            tags: Vec::new(),
        }
    }
    
    pub fn mark_done(&mut self) {
        self.completed = true;
        self.completed_at = Some(Utc::now());
    }
    
    pub fn is_overdue(&self) -> bool {
        if let Some(due) = self.due_date {
            !self.completed && due < Utc::now()
        } else {
            false
        }
    }
    
    pub fn set_priority(&mut self, priority: Priority) {
        self.priority = priority;
    }
    
    pub fn set_due_date(&mut self, due_date: DateTime<Utc>) {
        self.due_date = Some(due_date);
    }
    
    pub fn set_content(&mut self, content: String) {
        self.content = content;
    }
    
    pub fn add_tag(&mut self, tag: String) {
        if !self.tags.contains(&tag) {
            self.tags.push(tag);
        }
    }
    
    pub fn remove_tag(&mut self, tag: &str) {
        self.tags.retain(|t| t != tag);
    }
}
```

#### `src/models/task_list.rs` - 任务列表模型

```rust
use super::task::Task;
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TaskList {
    pub tasks: Vec<Task>,
    pub next_id: u64,
}

impl Default for TaskList {
    fn default() -> Self {
        TaskList {
            tasks: Vec::new(),
            next_id: 1,
        }
    }
}

impl TaskList {
    pub fn new() -> Self {
        Self::default()
    }
    
    pub fn add_task(&mut self, content: String) -> u64 {
        let id = self.next_id;
        self.next_id += 1;
        let task = Task::new(id, content);
        self.tasks.push(task);
        id
    }
    
    pub fn get_task(&self, id: u64) -> Option<&Task> {
        self.tasks.iter().find(|t| t.id == id)
    }
    
    pub fn get_task_mut(&mut self, id: u64) -> Option<&mut Task> {
        self.tasks.iter_mut().find(|t| t.id == id)
    }
    
    pub fn remove_task(&mut self, id: u64) -> Option<Task> {
        if let Some(pos) = self.tasks.iter().position(|t| t.id == id) {
            Some(self.tasks.remove(pos))
        } else {
            None
        }
    }
    
    pub fn complete_task(&mut self, id: u64) -> Option<()> {
        let task = self.get_task_mut(id)?;
        task.mark_done();
        Some(())
    }
    
    pub fn filter_completed(&self) -> Vec<&Task> {
        self.tasks.iter().filter(|t| t.completed).collect()
    }
    
    pub fn filter_pending(&self) -> Vec<&Task> {
        self.tasks.iter().filter(|t| !t.completed).collect()
    }
    
    pub fn filter_by_priority(&self, priority: super::task::Priority) -> Vec<&Task> {
        self.tasks.iter().filter(|t| t.priority == priority).collect()
    }
    
    pub fn search(&self, keyword: &str) -> Vec<&Task> {
        let keyword_lower = keyword.to_lowercase();
        self.tasks.iter()
            .filter(|t| t.content.to_lowercase().contains(&keyword_lower))
            .collect()
    }
    
    pub fn sort_by_created(&mut self, descending: bool) {
        if descending {
            self.tasks.sort_by(|a, b| b.created_at.cmp(&a.created_at));
        } else {
            self.tasks.sort_by(|a, b| a.created_at.cmp(&b.created_at));
        }
    }
    
    pub fn sort_by_priority(&mut self) {
        self.tasks.sort_by(|a, b| b.priority.cmp(&a.priority));
    }
    
    pub fn len(&self) -> usize {
        self.tasks.len()
    }
    
    pub fn is_empty(&self) -> bool {
        self.tasks.is_empty()
    }
}
```

#### `src/models/mod.rs` - 模型模块入口

```rust
pub mod task;
pub mod task_list;

pub use task::{Priority, Task};
pub use task_list::TaskList;
```

#### `src/cli.rs` - CLI 定义

```rust
use clap::{Parser, Subcommand, ValueEnum};

#[derive(Parser)]
#[command(
    name = "rustodo",
    about = "A simple and efficient command-line task manager",
    author = "Your Name",
    version = "0.1.0"
)]
pub struct Cli {
    #[command(subcommand)]
    pub command: Commands,
    
    /// 启用 verbose 输出
    #[arg(short, long)]
    pub verbose: bool,
    
    /// 指定数据文件路径
    #[arg(short, long, value_name = "FILE")]
    pub file: Option<std::path::PathBuf>,
}

#[derive(Subcommand)]
pub enum Commands {
    /// 添加新任务
    Add {
        /// 任务内容
        content: String,
        
        /// 设置优先级
        #[arg(short, long, value_enum)]
        priority: Option<PriorityArg>,
        
        /// 设置截止日期 (YYYY-MM-DD)
        #[arg(short, long)]
        due: Option<String>,
        
        /// 添加标签
        #[arg(short, long)]
        tag: Vec<String>,
    },
    
    /// 列出所有任务
    List {
        /// 只显示未完成的任务
        #[arg(short, long)]
        pending: bool,
        
        /// 只显示已完成的任务
        #[arg(short, long)]
        completed: bool,
        
        /// 按优先级筛选
        #[arg(short, long, value_enum)]
        priority: Option<PriorityArg>,
    },
    
    /// 标记任务完成
    Done {
        /// 任务 ID
        id: u64,
    },
    
    /// 删除任务
    Delete {
        /// 任务 ID
        id: u64,
    },
    
    /// 编辑任务
    Edit {
        /// 任务 ID
        id: u64,
        
        /// 新内容
        #[arg(short, long)]
        content: Option<String>,
        
        /// 设置优先级
        #[arg(short, long, value_enum)]
        priority: Option<PriorityArg>,
        
        /// 设置截止日期
        #[arg(short, long)]
        due: Option<String>,
    },
    
    /// 搜索任务
    Search {
        /// 搜索关键字
        keyword: String,
    },
    
    /// 显示统计信息
    Stats,
}

#[derive(Copy, Clone, PartialEq, Eq, ValueEnum)]
#[arg(rename_all = "lowercase")]
pub enum PriorityArg {
    Low,
    Medium,
    High,
}

impl From<PriorityArg> for crate::models::Priority {
    fn from(p: PriorityArg) -> Self {
        match p {
            PriorityArg::Low => crate::models::Priority::Low,
            PriorityArg::Medium => crate::models::Priority::Medium,
            PriorityArg::High => crate::models::Priority::High,
        }
    }
}
```

#### `src/errors.rs` - 错误处理

```rust
use thiserror::Error;

#[derive(Error, Debug)]
pub enum AppError {
    #[error("Task not found: id={0}")]
    TaskNotFound(u64),
    
    #[error("Storage error: {0}")]
    Storage(String),
    
    #[error("Invalid date format: {0}. Expected YYYY-MM-DD")]
    InvalidDateFormat(String),
    
    #[error("IO error: {0}")]
    Io(#[from] std::io::Error),
    
    #[error("Serialization error: {0}")]
    Serialization(String),
}

pub type Result<T> = std::result::Result<T, AppError>;

impl serde::Serialize for AppError {
    fn serialize<S>(&self, serializer: S) -> Result<S::Ok, S::Error>
    where
        S: serde::Serializer,
    {
        serializer.serialize_str(self.to_string().as_ref())
    }
}
```

#### `src/lib.rs` - 库入口

```rust
pub mod cli;
pub mod errors;
pub mod models;
pub mod storage;

pub use errors::{AppError, Result};
```

#### `src/storage/file.rs` - 持久化存储

```rust
use crate::errors::{AppError, Result};
use crate::models::TaskList;
use std::fs;
use std::path::Path;

/// 从文件加载任务列表
pub fn load(path: &Path) -> Result<TaskList> {
    if !path.exists() {
        return Ok(TaskList::new());
    }
    
    let content = fs::read_to_string(path)
        .map_err(|e| AppError::Storage(e.to_string()))?;
    
    // 尝试 JSON 格式
    if let Ok(list) = serde_json::from_str(&content) {
        return Ok(list);
    }
    
    // 尝试 YAML 格式
    if let Ok(list) = serde_yaml::from_str(&content) {
        return Ok(list);
    }
    
    // 格式不支持
    Err(AppError::Storage("Unknown file format".to_string()))
}

/// 保存任务列表到文件
pub fn save(path: &Path, list: &TaskList) -> Result<()> {
    // 确保目录存在
    if let Some(parent) = path.parent() {
        fs::create_dir_all(parent)
            .map_err(|e| AppError::Storage(e.to_string()))?;
    }
    
    // 默认使用 JSON 格式
    let content = serde_json::to_string_pretty(list)
        .map_err(|e| AppError::Serialization(e.to_string()))?;
    
    fs::write(path, content)
        .map_err(|e| AppError::Storage(e.to_string()))?;
    
    Ok(())
}

/// 获取默认数据文件路径
pub fn default_path() -> std::path::PathBuf {
    let home = dirs::home_dir().unwrap_or_else(|| std::path::PathBuf::from("."));
    home.join(".rustodo").join("tasks.json")
}
```

### 6. 快速运行验证

#### 添加 `dirs` 依赖

```bash
cargo add dirs@5
```

#### `src/main.rs` 草稿

```rust
use anyhow::Result;
use log::info;

fn main() -> Result<()> {
    // 初始化日志
    env_logger::Builder::from_env(env_logger::Env::default().default_filter_or("info"))
        .init();
    
    // 解析 CLI 参数
    let cli = rustodo::cli::Cli::parse();
    
    // 加载数据
    let data_path = cli.file.unwrap_or_else(rustodo::storage::file::default_path);
    let mut task_list = rustodo::storage::file::load(&data_path)?;
    
    info!("Loaded {} tasks from {:?}", task_list.len(), data_path);
    
    // 根据子命令执行操作（此处略去详细实现）
    match &cli.command {
        rustodo::cli::Commands::Add { content, .. } => {
            let id = task_list.add_task(content.clone());
            println!("Task added with id: {}", id);
        },
        rustodo::cli::Commands::List { .. } => {
            for task in &task_list.tasks {
                println!("[{}] {}", task.id, task.content);
            }
        },
        _ => {
            println!("Command not yet implemented");
        }
    }
    
    // 保存数据
    rustodo::storage::file::save(&data_path, &task_list)?;
    
    Ok(())
}
```

#### 编译验证

```bash
cargo build
#    Compiling rustodo v0.1.0
#     Finished dev [unoptimized + debuginfo] target(s)
```

#### 快速测试

```bash
# 尝试运行（会报错因为命令未完全实现，但能验证编译）
cargo run -- add "Test task from CLI"

# 查看帮助
cargo run -- --help
```

---

## 关键概念理解

### 项目结构设计原则

| 原则 | 说明 | 示例 |
|------|------|------|
| **模块化** | 按功能划分模块 | `commands/`, `models/`, `storage/` |
| **单一职责** | 每个模块只做一件事 | `task.rs` 只处理 Task 模型 |
| **依赖注入** | 通过参数传递依赖而非全局状态 | `load(path: &Path)` |
| **错误抽象** | 统一错误类型，便于处理 | `AppError` 枚举 |

### 依赖管理最佳实践

1. **固定主版本**：使用 `clap = "4"` 而非 `"*"`，确保 API 兼容性
2. **最小化依赖**：只添加真正需要的依赖
3. **开发依赖分离**：`[dev-dependencies]` 用于测试工具
4. **条件编译**：使用 `features` 按需启用功能

### 数据存储设计

- **优先级**：先 JSON 后 YAML 兼容
- **路径管理**：使用 `dirs` crate 获取跨平台路径
- **容错处理**：文件不存在时返回空列表而非报错

---

## 练习题

### 练习 1：完善 `src/main.rs`

完成 `main.rs` 的编写，处理所有 CLI 子命令：

```rust
// 提示：需要处理 add, list, done, delete, edit, search, stats 命令
// 并正确加载和保存数据
```

<details>
<summary>参考答案</summary>

```rust
use anyhow::Result;
use clap::Parser;
use log::info;

fn main() -> Result<()> {
    env_logger::Builder::from_env(env_logger::Env::default().default_filter_or("info"))
        .init();
    
    let cli = rustodo::cli::Cli::parse();
    let data_path = cli.file.unwrap_or_else(rustodo::storage::file::default_path);
    let mut task_list = rustodo::storage::file::load(&data_path)?;
    
    match &cli.command {
        rustodo::cli::Commands::Add { content, priority, due, tag } => {
            let id = task_list.add_task(content.clone());
            if let Some(p) = priority {
                if let Some(task) = task_list.get_task_mut(id) {
                    task.set_priority(rustodo::models::Priority::from(*p));
                }
            }
            println!("Task added with id: {}", id);
        },
        
        rustodo::cli::Commands::List { pending, completed, priority } => {
            let tasks: Vec<_> = task_list.tasks.iter().collect();
            let filtered: Vec<_> = match (pending, completed, priority) {
                (true, _, _) => tasks.iter().filter(|t| !t.completed).copied().collect(),
                (_, true, _) => tasks.iter().filter(|t| t.completed).copied().collect(),
                (_, _, Some(p)) => tasks.iter().filter(|t| {
                    use rustodo::models::Priority;
                    t.priority == Priority::from(*p)
                }).copied().collect(),
                _ => tasks,
            };
            
            for task in filtered {
                let status = if task.completed { "✓" } else { " " };
                println!("[{}] [{}] {}", status, task.id, task.content);
            }
        },
        
        rustodo::cli::Commands::Done { id } => {
            if task_list.complete_task(*id).is_some() {
                println!("Task {} marked as done", id);
            } else {
                anyhow::bail!("Task {} not found", id);
            }
        },
        
        rustodo::cli::Commands::Delete { id } => {
            if task_list.remove_task(*id).is_some() {
                println!("Task {} deleted", id);
            } else {
                anyhow::bail!("Task {} not found", id);
            }
        },
        
        rustodo::cli::Commands::Search { keyword } => {
            let results = task_list.search(keyword);
            for task in results {
                println!("[{}] {}", task.id, task.content);
            }
        },
        
        rustodo::cli::Commands::Stats => {
            let total = task_list.len();
            let done = task_list.filter_completed().len();
            let pending = total - done;
            println!("Total: {}, Done: {}, Pending: {}", total, done, pending);
        },
        
        _ => {
            println!("Command not yet fully implemented");
        }
    }
    
    rustodo::storage::file::save(&data_path, &task_list)?;
    Ok(())
}
```

</details>

### 练习 2：添加 `due` 命令实现

为带截止日期的 `add` 命令完善日期解析：

```rust
// 解析 YYYY-MM-DD 格式的日期字符串
fn parse_date(date_str: &str) -> Result<chrono::DateTime<Utc>> {
    // 提示：使用 chrono 的 DateTime::parse_from_str
}
```

<details>
<summary>参考答案</summary>

```rust
use chrono::{DateTime, NaiveDateTime, TimeZone, Utc};

fn parse_date(date_str: &str) -> Result<DateTime<Utc>, String> {
    let naive = NaiveDateTime::parse_from_str(date_str, "%Y-%m-%d")
        .map_err(|e| format!("Invalid date format: {}", e))?;
    Ok(Utc.from_utc_datetime(&naive))
}
```

</details>

### 练习 3：编写单元测试

为 `TaskList` 编写单元测试：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    
    #[test]
    fn test_add_task() {
        let mut list = TaskList::new();
        let id = list.add_task("Test task".to_string());
        
        assert_eq!(id, 1);
        assert_eq!(list.len(), 1);
    }
    
    // 添加更多测试：complete, delete, search...
}
```

<details>
<summary>参考答案</summary>

```rust
#[cfg(test)]
mod tests {
    use super::*;
    
    #[test]
    fn test_add_task() {
        let mut list = TaskList::new();
        let id = list.add_task("Test task".to_string());
        
        assert_eq!(id, 1);
        assert_eq!(list.len(), 1);
    }
    
    #[test]
    fn test_complete_task() {
        let mut list = TaskList::new();
        list.add_task("Task 1".to_string());
        list.add_task("Task 2".to_string());
        
        list.complete_task(1);
        
        let task = list.get_task(1).unwrap();
        assert!(task.completed);
    }
    
    #[test]
    fn test_remove_task() {
        let mut list = TaskList::new();
        list.add_task("Task 1".to_string());
        list.add_task("Task 2".to_string());
        
        let removed = list.remove_task(1);
        assert!(removed.is_some());
        assert_eq!(list.len(), 1);
    }
    
    #[test]
    fn test_search() {
        let mut list = TaskList::new();
        list.add_task("Buy groceries".to_string());
        list.add_task("Buy books".to_string());
        list.add_task("Walk dog".to_string());
        
        let results = list.search("buy");
        assert_eq!(results.len(), 2);
    }
}
```

</details>

### 练习 4：添加标签功能

在 `Task` 中添加标签功能的相关方法，并测试：

```rust
impl Task {
    // 添加 add_tag, remove_tag, has_tag 方法
}
```

<details>
<summary>参考答案</summary>

```rust
impl Task {
    pub fn add_tag(&mut self, tag: String) {
        if !self.tags.contains(&tag) {
            self.tags.push(tag);
        }
    }
    
    pub fn remove_tag(&mut self, tag: &str) {
        self.tags.retain(|t| t != tag);
    }
    
    pub fn has_tag(&self, tag: &str) -> bool {
        self.tags.iter().any(|t| t == tag)
    }
}
```

</details>

### 练习 5：集成测试

编写集成测试验证完整的添加和查询流程：

```rust
#[test]
fn test_add_and_list_integration() {
    // 使用 assert_cmd 创建子进程测试
}
```

<details>
<summary>参考答案</summary>

```rust
#[test]
fn test_add_and_list_integration(tmp_path: TempDir) {
    let data_file = tmp_path.path().join("tasks.json");
    
    // 添加任务
    let mut cmd = Command::cargo_bin("rustodo").unwrap();
    cmd.arg("--file").arg(&data_file)
       .arg("add")
       .arg("Test integration task");
    cmd.assert().success();
    
    // 列出任务
    let mut cmd = Command::cargo_bin("rustodo").unwrap();
    cmd.arg("--file").arg(&data_file)
       .arg("list");
    cmd.assert()
        .success()
        .stdout(predicate::str::contains("Test integration task"));
}
```

</details>

---

## 明日计划

明天我们将完成 `rustodo` 的核心功能实现：

- **Day 71**：完善 `add`、`list` 命令的输出格式
- 实现优先级和截止日期的显示
- 添加彩色输出和格式化

---

## 参考资源

| 资源 | 链接 |
|------|------|
| Clap 文档 | <https://docs.rs/clap/latest/clap/> |
| Chrono 文档 | <https://docs.rs/chrono/latest/chrono/> |
| Rust 项目布局 | <https://doc.rust-lang.org/book/ch12-03-improving-error-handling-and-modularity.html> |
| 集成测试指南 | <https://doc.rust-lang.org/book/ch11-03-test-organization.html> |
| Rust Todo 项目示例 | <https://github.com/LukeMathWalker/rust-todo-backend> |

---

## 今日感悟

经过漫长的理论学习，今天终于开始动手做项目了。最大的感受是：

1. **纸上得来终觉浅**——写一个完整的 CLI 工具需要考虑太多细节，代码组织、错误处理、数据持久化……
2. **模块化的价值**——好的项目结构让代码易于维护和测试
3. **依赖管理是艺术**——选择合适的依赖，控制依赖数量，是工程化的关键
4. **测试驱动开发**——先写测试再实现功能，能大大提高代码质量

`rustodo` 只是一个起点，但它将 Rust 的各个知识点串联起来。从明天开始，我们会让它真正运转起来。

加油！

---

*第 13 周 · 实战项目 · 第 1 天完成*